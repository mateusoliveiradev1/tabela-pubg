{
  "name": "@pubg-camp/discord-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsup src/main.ts --format esm --clean --external fastify --external discord.js",
    "clean": "rimraf dist coverage",
    "dev": "tsx watch src/main.ts",
    "lint": "biome check src",
    "start": "node dist/main.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@pubg-camp/config": "workspace:*",
    "@pubg-camp/contracts": "workspace:*",
    "@pubg-camp/logger": "workspace:*",
    "@pubg-camp/observability": "workspace:*",
    "discord.js": "14.27.0",
    "fastify": "5.12.1",
    "zod": "catalog:"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
