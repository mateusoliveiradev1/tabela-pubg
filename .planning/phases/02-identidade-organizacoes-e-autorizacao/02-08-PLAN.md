---
phase: 02-identidade-organizacoes-e-autorizacao
plan: 08
type: execute
wave: 8
depends_on: [02-07]
files_modified:
  - apps/worker/package.json
  - pnpm-lock.yaml
  - apps/worker/src/notifications/email-sender.ts
  - apps/worker/src/notifications/resend-email-sender.ts
  - apps/worker/src/notifications/processor.ts
  - apps/worker/src/notifications/processor.spec.ts
  - apps/worker/src/notifications/resend-email-sender.contract.spec.ts
  - apps/worker/src/notifications/templates.ts
  - apps/worker/src/main.ts
autonomous: true
requirements: [AUTH-002, AUTH-003, AUTH-006, ORG-002, NFR-005]
user_setup:
  - service: resend
    why: "Entregar OTP, convite e alerta de novo dispositivo"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard -> API Keys"
      - name: EMAIL_FROM
        source: "Remetente de domínio verificado no Resend"
    dashboard_config:
      - task: "Verificar domínio e remetente transacional"
        location: "Resend Dashboard -> Domains"
must_haves:
  truths:
    - "OTP, convite e alerta de novo device são enviados depois do commit e retry não duplica envio."
    - "Ciphertext é descriptografado somente no worker e apagado após sucesso/expiração."
    - "E-mail de novo acesso abre tela de sessões sem encerrar nada por GET."
    - "Create/resend de convite usa idempotency diferente por delivery; retry do mesmo delivery nunca duplica envio."
  artifacts:
    - path: "apps/worker/src/notifications/processor.ts"
      provides: "Processor idempotente de notification delivery"
    - path: "apps/worker/src/notifications/templates.ts"
      provides: "Templates OTP, convite e novo dispositivo alinhados ao UI-SPEC"
  key_links:
    - from: "apps/worker/src/notifications/processor.ts"
      to: "packages/database/src/repositories/notifications.ts"
      via: "deliveryId do job"
      pattern: "deliveryId"
---

<objective>
Entregar e-mails transacionais de forma assíncrona, cifrada e idempotente.
Purpose: cumprir D-07 sem acoplar disponibilidade do provedor aos requests HTTP.
Output: EmailSender port, adapter Resend e processor BullMQ testado.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-RESEARCH.md
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-UI-SPEC.md
@apps/worker/src/main.ts
@packages/queue/src/index.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Criar processor idempotente e templates seguros</name>
  <files>apps/worker/src/notifications/email-sender.ts, apps/worker/src/notifications/processor.ts, apps/worker/src/notifications/processor.spec.ts, apps/worker/src/notifications/templates.ts, apps/worker/src/notifications/resend-email-sender.contract.spec.ts</files>
  <behavior>
    - Job recebe somente deliveryId; delivery sent/expired não envia de novo.
    - Falha transitória preserva envelope para retry; sucesso grava provider id e apaga ciphertext.
    - Novo dispositivo contém device/horário/local aproximado, aviso antifraude e CTA read-only conforme UI-SPEC.
    - Invitation create/resend entrega o token correto do envelope; retry repete idempotency key, resend usa delivery/key nova.
  </behavior>
  <action>Definir EmailSender com idempotencyKey estável. Processor carrega delivery, valida status/expiry, descriptografa AES-GCM no último momento, renderiza template tipado, envia e finaliza atomicamente. Template de convite recebe token somente do envelope e retry mantém key; resend é novo delivery. Template de alerta recebe token opaco cujo digest está user-bound no banco; GET apenas resolve estado. Templates mascaram e-mail e nunca incluem OTP/session token/IP completo. Contract test captura requests Resend sem rede e prova headers/idempotency/body.</action>
  <verify><automated>rtk pnpm --filter @pubg-camp/worker test -- notifications/processor resend-email-sender.contract</automated></verify>
  <done>Retry, duplicate job, expiry e cleanup estão provados sem rede.</done>
</task>

<task type="auto">
  <name>Task 2: Conectar adapter Resend e lifecycle do worker</name>
  <files>apps/worker/package.json, pnpm-lock.yaml, apps/worker/src/notifications/resend-email-sender.ts, apps/worker/src/main.ts</files>
  <action>Instalar `resend` aprovado e adicionar `@pubg-camp/database` workspace. Implementar adapter usando idempotency key. Compor config/database/queue/adapter no main, substituir placeholder e fechar worker, Redis, banco e telemetria. Não logar recipient completo/body/ciphertext/API key.</action>
  <verify><automated>rtk pnpm --filter @pubg-camp/worker lint &amp;&amp; rtk pnpm --filter @pubg-camp/worker typecheck &amp;&amp; rtk pnpm --filter @pubg-camp/worker test &amp;&amp; rtk pnpm --filter @pubg-camp/worker build</automated></verify>
  <done>Worker entrega mensagens confirmadas sem duplicidade e encerra recursos corretamente.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| DB envelope→worker | Segredo curto é descriptografado em memória. |
| worker→Resend | Conteúdo transacional sai ao provedor externo. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-18 | Information Disclosure | delivery payload | mitigate | AES-GCM, TTL, redaction e cleanup. |
| T-02-19 | Repudiation | retry | mitigate | Idempotency key e estado/provider id persistido. |
| T-02-SC | Tampering | resend install | mitigate | Pacote aprovado/slopcheck OK em 02-RESEARCH. |
</threat_model>

<verification>
- `rtk pnpm --filter @pubg-camp/worker test`
- `rtk pnpm turbo run lint typecheck build --filter=@pubg-camp/worker`
</verification>

<success_criteria>
- D-07 gera e-mail idempotente com copy e privacidade do UI-SPEC.
- API não importa Resend nem envia e-mail inline.
</success_criteria>

<output>Create `.planning/phases/02-identidade-organizacoes-e-autorizacao/02-08-SUMMARY.md` when done.</output>
