---
phase: 02-identidade-organizacoes-e-autorizacao
plan: 39
type: execute
wave: 11
depends_on: [02-32, 02-33, 02-36, 02-37, 02-41, 02-42]
files_modified:
  - apps/web/e2e/phase2-runtime.spec.ts
  - apps/web/playwright.config.ts
  - scripts/run-phase2-e2e.mjs
  - apps/api/src/e2e/provider-mode.spec.ts
  - apps/web/package.json
  - package.json
  - scripts/validate-phase2-ci.mjs
  - .github/workflows/ci.yml
autonomous: true
gap_closure: true
requirements: [AUTH-001, AUTH-002, AUTH-003, AUTH-004, AUTH-005, AUTH-006, ORG-001, ORG-002, ORG-003, ORG-004, ORG-005, AUD-001, NFR-005]
must_haves:
  truths:
    - "O harness sobe Next+BFF, Nest, PostgreSQL, Redis, outbox publisher, BullMQ consumers, worker e browser reais; somente providers externos usam os adapters confinados de 02-33."
    - "Cada execução usa um E2E_RUN_ID validado e não amplo como fonte única de schema, Redis/BullMQ prefix e object/mail root, e limpa exatamente esse escopo mesmo em falha."
    - "Fresh-checkout CI executa o harness real-stack com PostgreSQL e Redis antes de poder concluir success."
    - "Failure, timeout, cancelled, neutral ou skipped do gate runtime bloqueia o workflow."
    - "A CI preserva suites já satisfeitas e exige cleanup run-scoped sem expor segredos ou mailbox."
    - "O runtime prova D-10 com expiração exata de sete dias, aceite único, replay negado, e-mail verificado exato e aplicação completa do papel/cargos iniciais."
    - "O runtime prova D-16: mudança de papel, revogação e transferência de propriedade exigem reautenticação fresca e escrevem a auditoria na mesma transação da mutação."
    - "O runtime prova D-17: owner/admin enxergam a auditoria completa da organização e membro enxerga somente suas próprias ações."
  artifacts:
    - path: "scripts/run-phase2-e2e.mjs"
      provides: "Lifecycle hermético e identificador canônico do stack real"
    - path: "apps/web/e2e/phase2-runtime.spec.ts"
      provides: "Browser/API assertions das ligações runtime"
    - path: ".github/workflows/ci.yml"
      provides: "Gate obrigatório PostgreSQL+Redis+Nest+worker+browser"
    - path: "scripts/validate-phase2-ci.mjs"
      provides: "Validador fail-closed contra regressão sintética/skipped"
    - path: "apps/api/src/e2e/provider-mode.spec.ts"
      provides: "Feedback rápido da composição de providers confinados usada pelo lifecycle real"
  key_links:
    - from: "scripts/run-phase2-e2e.mjs"
      to: "apps/api + apps/worker + apps/web"
      via: "real process entrypoints, readiness and exact E2E_RUN_ID propagation"
      pattern: "E2E_RUN_ID|@pubg-camp/api|@pubg-camp/worker|next"
    - from: ".github/workflows/ci.yml"
      to: "scripts/run-phase2-e2e.mjs"
      via: "mandatory runtime command after integration"
      pattern: "test:e2e:runtime"
---

<objective>
Construir o harness runtime/browser real sobre os adapters de 02-33 e torná-lo um gate obrigatório da CI.
Purpose: provar as conexões runtime mascaradas pelos testes sintéticos e impedir que uma fresh checkout conclua Phase 2 sem essa evidência.
Output: harness/browser run-scoped, scripts públicos, workflow com services e validador estático fail-closed.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-33-SUMMARY.md
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-41-SUMMARY.md
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-42-SUMMARY.md
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-32-SUMMARY.md
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-36-SUMMARY.md
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-37-SUMMARY.md
@scripts/run-phase2-e2e.mjs
@apps/web/e2e/phase2-runtime.spec.ts
@apps/web/playwright.config.ts
@package.json
@apps/web/package.json
@.github/workflows/ci.yml
@scripts/validate-phase2-ci.mjs
@apps/api/src/e2e/provider-mode.spec.ts

<interfaces>
The mandatory lifecycle is preflight→isolated schema/migrations→seed→real API+worker+web+second-origin image server→Playwright→database/queue assertions→graceful stop→scoped cleanup. The mandatory command is `rtk pnpm test:e2e:runtime`; synthetic `--serve-api` is forbidden.

`runtime.runScopeId` is the exact `E2E_RUN_ID` propagated to every child and cleanup operation. It must be 19-67 ASCII characters matching `^run-[a-z0-9][a-z0-9-]{14,62}$`, and must not match the broad prefix `^run-(all|any|default|shared|global|public|phase2|e2e|test)(-|$)`. If no environment value is supplied, the only generated form is `run-${randomBytes(12).toString("hex")}`, exactly 28 characters matching `^run-[0-9a-f]{24}$`; CI supplies the value from `.github/workflows/ci.yml#jobs.phase2-e2e.env.E2E_RUN_ID`. The harness emits this non-secret value and source for 02-34 evidence. Cleanup schema, Redis/BullMQ prefix and object/mail root scope IDs equal it byte-for-byte.
CI ordering is migrations/integration → runtime E2E → broader browser/regression gates.
Synthetic `--serve-api` is forbidden from mandatory smoke/runtime evidence.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Exercitar as ligações runtime no browser e na infraestrutura</name>
  <files>apps/web/e2e/phase2-runtime.spec.ts, apps/web/playwright.config.ts, scripts/run-phase2-e2e.mjs, apps/api/src/e2e/provider-mode.spec.ts</files>
  <read_first>
    - .planning/phases/02-identidade-organizacoes-e-autorizacao/02-33-SUMMARY.md
    - scripts/run-phase2-e2e.mjs
    - scripts/seed-phase2-e2e.mjs
    - apps/web/e2e/phase2-smoke.spec.ts
    - apps/web/e2e/phase2-accessibility.spec.ts
    - apps/web/playwright.config.ts
    - apps/web/src/components/identity/email-otp-form.tsx
    - apps/web/src/components/identity/device-session-card.tsx
    - apps/web/src/components/identity/identity-cards.tsx
    - apps/web/src/components/organizations/invitation-list.tsx
    - apps/api/src/e2e/provider-mode.spec.ts
    - apps/api/src/organizations/invitations.controller.ts
    - apps/api/src/organizations/members.controller.ts
    - apps/api/src/audit/audit.controller.ts
  </read_first>
  <behavior>
    - Harness replaces the synthetic `--serve-api` path with real Nest and worker entrypoints, waits for PostgreSQL, Redis, publisher and consumers, then drives the existing Next+BFF through Playwright.
    - Browser proves provisional Discord→bound verified-email OTP→replacement trusted cookie/CSRF, same-tenant organization work, cross-tenant deny, invitation/outbox/worker, session revoke and second-origin logo under CSP using real data transitions.
    - The actual run scope is non-empty, 19-67 characters, matches `^run-[a-z0-9][a-z0-9-]{14,62}$`, is not a broad sentinel, comes from validated E2E_RUN_ID or the exact 24-hex generator, and is propagated unchanged to all cleanup scope IDs.
    - Invitation runtime proof creates an invitation whose `expires_at` is exactly issued-at plus seven days, accepts it once only with the exact verified email before expiry, applies the declared initial organization role plus every declared operational assignment, rejects replay without duplicate membership/audit/outbox, and rejects the same token at/after expiry or under a different verified email.
    - Role change, member revocation and ownership transfer each deny without fresh reauthentication and without mutation/audit; after fresh reauthentication each succeeds and its mutation plus one redacted reasoned audit row become visible together, never separately.
    - Owner and admin sessions see complete organization audit history; an ordinary member sees only events whose actor is that member, and filters/query input cannot widen visibility.
    - Fast lifecycle self-tests, the exact provider spec and the exact runtime Playwright selector all fail nonzero when their owned cases are absent; full real-stack execution remains the wave gate after task feedback.
  </behavior>
  <action>Replace the in-memory `--serve-api` lifecycle with actual API and worker processes configured with the confined providers delivered by 02-33 and the one-scope identity Redis contract from 02-41. Add a fast `--self-test` lifecycle mode to `run-phase2-e2e.mjs` that exercises run-id validation, child readiness/order, timeout/failure unwinding and exact cleanup-target guards without starting product processes; it must assert a fixed nonzero named-case inventory and exit nonzero if any case is missing. Extend the exact owned `provider-mode.spec.ts` for API/worker/provider composition used by the harness. Start a separate second-origin local image server for CSP evidence, never a product BFF synthetic route. Add a runtime Playwright project/spec that first proves provisional organization creation/invitation acceptance are denied, then completes the bound verified-email OTP producer from 02-30/02-36, observes old-token rejection plus replacement trusted cookie/CSRF, and per D-04 proves that replacement session can create an organization. For D-10/D-11, seed/issue invitations under a controllable server clock and prove exactly seven-day expiry, exact verified-email acceptance before expiry, one-use/replay denial, and atomic application of the declared initial organization role plus every operational assignment. Per D-16, drive role change, membership revocation and ownership transfer first without then with fresh reauthentication; assert the negative paths write neither mutation nor audit and the positive paths expose mutation plus one transactional redacted audit record together. Per D-17, query the resulting history as owner, admin and member and prove full/full/self-only visibility. Per D-12 preserve last owner absent explicit transfer; per D-13/D-14 prove organization-wide owner/admin behavior and strict accumulated championship-role permission union without implicit promotion. Validate E2E_RUN_ID before resource creation, pass it byte-for-byte to both identity Redis prefixes, BullMQ and every child/cleanup scope, seed a foreign-run Redis sentinel, and prove cleanup removes only the current run's keys while preserving that sentinel; refuse broad, mismatched or escaped targets.</action>
  <acceptance_criteria>
    - Assertions prove transitions in real PostgreSQL/Redis/BullMQ and process readiness proves real API, publisher, worker and consumers before browser start.
    - Same-tenant mutation succeeds; cross-tenant mutation has no DB side effect and exactly one redacted security record.
    - Tests reject absent, empty, broad, malformed, under-19 and over-67 E2E_RUN_ID values before resource creation, while the default generator is exactly `run-` plus 24 lowercase hex characters.
    - Cleanup identifiers for schema, Redis/BullMQ and object/mail root are derived from and equal the validated runScopeId.
    - Runtime assertions prove D-04 only after the OTP promotion producer, prove D-11 exact-email acceptance and prove D-13 organization-wide versus championship-scoped behavior through the real stack.
    - D-10 assertions cover issued+7d boundary, pre-expiry success, at/after-expiry denial, one winner/replay denial, exact email, initial role and every requested operational assignment with no duplicate side effects.
    - D-16 assertions cover all three sensitive member/ownership commands with stale/missing reauthentication denial and fresh success, and prove audit/mutation transactional visibility; D-17 assertions cover owner/admin full versus member self-only pages/API results.
    - After finally cleanup, no current-run auth-limiter, PKCE or BullMQ key remains, while the foreign-run sentinel is still readable and is removed only by its owning fixture.
    - Lifecycle `--self-test`, exact provider Vitest selector and exact Playwright `--list` selector each report a nonzero expected case count and return nonzero on zero/missing cases; none uses `--passWithNoTests` or conditional skips.
  </acceptance_criteria>
  <verify><automated>rtk node scripts/run-phase2-e2e.mjs --self-test &amp;&amp; rtk pnpm --filter @pubg-camp/api exec vitest run src/e2e/provider-mode.spec.ts &amp;&amp; rtk pnpm --filter @pubg-camp/web exec playwright test e2e/phase2-runtime.spec.ts --list</automated></verify>
  <cleanup>Stop children in reverse order with graceful timeout; in `finally`, drop only the validated run schema, delete only the exact run-prefixed Redis/BullMQ namespace and remove only the resolved mkdtemp root. Refuse broad, invalid, mismatched or escaped targets.</cleanup>
  <done>Browser and infrastructure evidence prove the complete BFF→Nest→PostgreSQL→BullMQ→worker flow under one concrete run scope.</done>
</task>

<task type="auto">
  <name>Task 2: Tornar o gate real obrigatório na CI</name>
  <files>apps/web/package.json, package.json, scripts/validate-phase2-ci.mjs, .github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/02-identidade-organizacoes-e-autorizacao/02-33-SUMMARY.md
    - package.json
    - apps/web/package.json
    - .github/workflows/ci.yml
    - scripts/validate-phase2-ci.mjs
    - .planning/phases/02-identidade-organizacoes-e-autorizacao/02-VALIDATION.md
  </read_first>
  <action>Add explicit workspace/root `test:e2e:runtime` scripts and execute the completed Task 1 harness in the existing `phase2-e2e` job after migration/integration and before broader browser regression steps, using PostgreSQL/Redis services and fresh-checkout builds. Preserve the tracked canonical expanded mandatory CI job set and names: `quality`→`Quality and affected build`; `phase2-integration[suite=integration]`→`Phase 2 integration`; `phase2-integration[suite=concurrent]`→`Phase 2 concurrent`; `phase2-e2e`→`Phase 2 browser E2E`; and `images[app=web|api|worker|discord-bot]`→`Image web|api|worker|discord-bot`, cardinality exactly 8. Add a `--self-test` mode to the owned CI validator with a fixed nonzero named negative-case inventory; it must reject missing runtime command, zero/removed owned lifecycle/provider/runtime selectors, conditional skips, `--passWithNoTests`, each missing key/matrix/name pair, missing Redis preflight, missing API/worker/publisher process, absent cleanup and skippable mandatory jobs. The normal validator fails on the same conditions. Keep push/pull_request, Chromium, secret scan, package builds, authorization, last-owner, invitation concurrency, secret-boundary, PKCE and accessibility suites required. Do not alter visual styling/copy reserved for Phase 2.1.</action>
  <acceptance_criteria>
    - Static validator proves real API+worker gate, PostgreSQL+Redis services, integration/runtime/browser ordering and no synthetic mandatory API.
    - Static validator derives and locks the exact eight expanded key/matrix/name pairs from the tracked workflow; evidence cannot declare a smaller or renamed replacement set.
    - Existing satisfied-requirement suites remain required.
    - Failure, timeout, cancelled, neutral or skipped runtime gate blocks the workflow, and cleanup runs even on failure.
    - Validator self-test reports its expected named negative-case count and fails if a fixture becomes vacuously valid or no cases execute.
  </acceptance_criteria>
  <verify><automated>rtk node scripts/validate-phase2-ci.mjs --self-test .github/workflows/ci.yml &amp;&amp; rtk node scripts/validate-phase2-ci.mjs .github/workflows/ci.yml &amp;&amp; rtk pnpm verify:phase2-ci</automated></verify>
  <cleanup>CI cleanup removes only the validated run schema, exact Redis/BullMQ prefix and mkdtemp-owned roots; failure artifacts exclude env, provider tokens and mailbox contents.</cleanup>
  <done>Fresh-checkout CI cannot pass Phase 2 without the real-stack runtime and browser evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| browser→BFF→Nest→infrastructure | Real HTTP/cookie/CSRF/tenant behavior must reach real persistence and workers. |
| harness→shared infrastructure | Run-scope validation and exact equality constrain every cleanup target. |
| repository checkout→CI services | Workflow configuration decides which runtime and infrastructure are actually proved. |
| test runtime→CI artifacts | Failure evidence must remain useful without exposing secrets. |

## ASVS / STRIDE Threat Register
| Threat ID | ASVS | STRIDE | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-98 | V15 | Tampering | synthetic/skipped CI gate | mitigate | Static validator, mandatory dependencies and fail-closed conclusions. |
| T-02-99 | V13/V14 | Information Disclosure | CI cleanup/artifacts | mitigate | Exact run scoping, always cleanup and artifact exclusion. |
| T-02-105 | V13/V15 | Tampering/DoS | broad or mismatched cleanup scope | mitigate | Validated 19-67 character E2E_RUN_ID, broad-sentinel denylist and byte-equal cleanup scope IDs. |
</threat_model>

<verification>
- Fast task feedback: `rtk node scripts/run-phase2-e2e.mjs --self-test`; `rtk pnpm --filter @pubg-camp/api exec vitest run src/e2e/provider-mode.spec.ts`; `rtk pnpm --filter @pubg-camp/web exec playwright test e2e/phase2-runtime.spec.ts --list`; `rtk node scripts/validate-phase2-ci.mjs --self-test .github/workflows/ci.yml`.
- Wave gate: `rtk pnpm phase2:integration:preflight &amp;&amp; rtk pnpm test:e2e:runtime` against real PostgreSQL and Redis.
- Phase gate: `rtk pnpm ci:verify` and the tracked push CI mandatory eight-job set.
- `rtk node scripts/validate-phase2-ci.mjs .github/workflows/ci.yml`
- `rtk pnpm verify:phase2-ci`
</verification>

<success_criteria>A CI prova o harness real-stack e não pode regredir para evidência sintética ou skipped.</success_criteria>
<output>Create `.planning/phases/02-identidade-organizacoes-e-autorizacao/02-39-SUMMARY.md` when done.</output>
