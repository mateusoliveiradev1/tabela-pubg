---
phase: 02-identidade-organizacoes-e-autorizacao
plan: 33
type: execute
wave: 10
depends_on: [02-26, 02-28, 02-38, 02-41]
files_modified:
  - apps/api/src/main.ts
  - apps/api/src/e2e/provider-mode.spec.ts
  - apps/worker/src/e2e/file-email-sender.ts
  - apps/worker/src/e2e/file-email-sender.spec.ts
  - apps/worker/src/e2e/file-logo-storage.ts
  - apps/worker/src/e2e/file-logo-storage.spec.ts
  - apps/worker/src/main.ts
  - scripts/seed-phase2-e2e.mjs
autonomous: true
gap_closure: true
requirements: [AUTH-001, AUTH-002, AUTH-003, AUTH-004, AUTH-005, AUTH-006, ORG-001, ORG-002, ORG-003, ORG-004, ORG-005, AUD-001, NFR-005]
must_haves:
  truths:
    - "API e worker reais aceitam providers externos test-only somente sob a conjunção completa NODE_ENV=test, E2E_PROVIDER_MODE=fake, E2E_RUN_ID válido e raiz absoluta run-scoped."
    - "Seed cria schema, Redis/BullMQ prefix, object root e mailbox exclusivos sem imprimir credenciais, OTP ou convite."
    - "Produção continua exigindo Resend/S3 reais e falha ao inicializar sob configuração fake parcial ou raiz fora do escopo."
  artifacts:
    - path: "scripts/seed-phase2-e2e.mjs"
      provides: "Seed isolado para schema e namespace runtime"
    - path: "apps/worker/src/e2e/file-email-sender.ts"
      provides: "Provider de e-mail test-only confinado ao run root"
  key_links:
    - from: "apps/worker/src/main.ts"
      to: "apps/worker/src/e2e/file-email-sender.ts"
      via: "complete test-only provider conjunction"
      pattern: "E2E_PROVIDER_MODE|E2E_RUN_ID|E2E_OBJECT_ROOT"
---

<objective>
Confinar os providers externos test-only e preparar o seed isolado consumido pelo gate real.
Purpose: permitir prova hermética de API e worker reais sem expor provider, mailbox ou infraestrutura compartilhada.
Output: adapters externos test-only, composição API/worker e seed run-scoped.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-26-SUMMARY.md
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-28-SUMMARY.md
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-38-SUMMARY.md
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-41-SUMMARY.md
@scripts/seed-phase2-e2e.mjs

<interfaces>
External test adapters require the conjunction `NODE_ENV=test`, `E2E_PROVIDER_MODE=fake`, valid opaque E2E_RUN_ID and absolute mkdtemp-owned root.
02-39 owns the runtime/browser lifecycle that consumes these providers and the seed before wiring CI.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Confinar providers test-only e seed do runtime real</name>
  <files>apps/api/src/main.ts, apps/api/src/e2e/provider-mode.spec.ts, apps/worker/src/e2e/file-email-sender.ts, apps/worker/src/e2e/file-email-sender.spec.ts, apps/worker/src/e2e/file-logo-storage.ts, apps/worker/src/e2e/file-logo-storage.spec.ts, apps/worker/src/main.ts, scripts/seed-phase2-e2e.mjs</files>
  <read_first>
    - scripts/seed-phase2-e2e.mjs
    - apps/api/src/main.ts
    - apps/api/src/identity/identity.runtime.ts
    - apps/worker/src/main.ts
    - apps/api/src/organizations/adapters/e2e-organization-logo-storage.ts
    - apps/worker/src/notifications/email-sender.ts
    - apps/worker/src/outbox/publisher.ts
  </read_first>
  <behavior>
    - File email/storage adapters can start only under the complete test conjunction and write only beneath validated run root with restrictive permissions.
    - Seed creates an isolated PostgreSQL search_path plus run-scoped BullMQ/Redis prefix and object/mail directory; partial fake configuration fails closed.
    - A real API composition test uses a fake Discord provider under the complete test conjunction and proves `createIdentityRuntime` receives the validated E2E_RUN_ID-derived Redis auth/PKCE prefixes from 02-41; production/default composition retains stable production prefixes.
  </behavior>
  <action>Extend seed to apply 0000→latest into a unique schema and construct a process-local database URL/search_path without printing credentials. Add file-backed EmailSender and storage deleter only for the full E2E provider conjunction; API/worker must fail boot on partial fake flags, invalid run id or root outside the mkdtemp directory. Configure BullMQ prefix and pass the already validated E2E_RUN_ID into the 02-41 identity runtime scope contract; do not reconstruct or default the auth/PKCE prefixes in `main.ts`. Add `provider-mode.spec.ts` that boots the real API composition with a fake Discord provider and Redis client spies under the complete conjunction, asserts both scoped identity namespaces plus BullMQ share the same runScopeId, and proves production/default mode uses the stable production namespaces. Keep mailbox/object files beneath the validated root with restrictive permissions. Production boot continues to require real Resend/S3 settings.</action>
  <acceptance_criteria>
    - Partial fake configuration fails closed in API and worker tests; production path still requires real Resend/S3 settings.
    - No DATABASE_URL, Redis credential, OTP, invite token or mailbox file content is printed or uploaded as artifact.
    - API fake-provider test fails on absent/broad/mismatched run scope before Redis writes and observes no unscoped `pubg-camp:auth` or `pubg-camp:oauth:pkce` key in E2E mode.
  </acceptance_criteria>
  <verify><automated>rtk pnpm --filter @pubg-camp/worker exec vitest run src/e2e/file-email-sender.spec.ts src/e2e/file-logo-storage.spec.ts &amp;&amp; rtk pnpm --filter @pubg-camp/api exec vitest run src/e2e/provider-mode.spec.ts &amp;&amp; rtk pnpm phase2:integration:preflight</automated></verify>
  <cleanup>Tests remove only exact run-prefixed Redis keys/queues, drop only the validated run schema and remove only the resolved test root; refuse cleanup if any target escapes its run prefix/root.</cleanup>
  <done>Real API and worker entrypoints can consume isolated provider substitutes and seed state only under the complete test conjunction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| test harness→shared infrastructure | Cleanup must never target another run or shared production state. |

## ASVS / STRIDE Threat Register
| Threat ID | ASVS | STRIDE | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-89 | V14/V16 | Information Disclosure | test mail/log/artifact | mitigate | Run-root custody, redaction and failure-only safe artifacts. |
| T-02-90 | V13/V15 | DoS/Tampering | cleanup isolation | mitigate | Validated run prefixes, exact schema/root and finally cleanup. |
</threat_model>

<verification>
- `rtk pnpm phase2:integration`
- `rtk pnpm --filter @pubg-camp/worker exec vitest run src/e2e/file-email-sender.spec.ts src/e2e/file-logo-storage.spec.ts`
- `rtk pnpm --filter @pubg-camp/api exec vitest run src/e2e/provider-mode.spec.ts`
</verification>

<success_criteria>Providers test-only e seed são confinados por run e ficam prontos para o harness real de 02-39 sem abrir caminho sintético em produção.</success_criteria>
<output>Create `.planning/phases/02-identidade-organizacoes-e-autorizacao/02-33-SUMMARY.md` when done.</output>
