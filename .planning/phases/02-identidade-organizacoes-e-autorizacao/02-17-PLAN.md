---
phase: 02-identidade-organizacoes-e-autorizacao
plan: 17
type: execute
wave: 3
depends_on: [02-03]
files_modified: []
autonomous: false
requirements: [NFR-005]
user_setup:
  - service: integration-infrastructure
    why: "Provar disponibilidade inicial de PostgreSQL/Redis e migrations antes dos repositories"
    env_vars:
      - name: TEST_DATABASE_URL
        source: "PostgreSQL real descartável ou service container CI"
      - name: TEST_REDIS_URL
        source: "Redis real descartável ou service container CI"
must_haves:
  truths:
    - "02-04/02-05 não iniciam até PostgreSQL, Redis e migrations reais passarem no preflight inicial."
    - "Falha/ausência de qualquer serviço ou migration nunca conta como conclusão."
    - "Este gate não aprova o workflow CI final; essa responsabilidade pertence ao 02-24."
  artifacts: []
  key_links:
    - from: "scripts/run-phase2-integration.mjs"
      to: "TEST_DATABASE_URL + TEST_REDIS_URL"
      via: "preflight fail-closed abre conexões e aplica migrations"
      pattern: "phase2:integration:preflight"
---

<objective>
Bloquear a execução dos repositórios até infraestrutura real provar o preflight inicial.
Purpose: impedir que ausência local de PostgreSQL/Redis ou migrations incompletas seja confundida com sucesso.
Output: checkpoint de disponibilidade aprovado com URLs reais redigidas e preflight verde; nenhuma afirmação sobre CI final.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@scripts/run-phase2-integration.mjs
</context>

<tasks>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Aprovar e executar disponibilidade inicial real</name>
  <files>Nenhum arquivo é modificado neste checkpoint.</files>
  <action>Obter `TEST_DATABASE_URL` e `TEST_REDIS_URL` reais, redigir seus valores no log e executar somente o preflight inicial. Não fazer push, não disparar GitHub Actions e não alegar que o workflow final foi validado. Não liberar dependentes em caso de URL ausente, conexão falha, migration falha, serviço simulado ou preflight indisponível.</action>
  <what-built>Runner de preflight fail-closed para PostgreSQL/Redis e migrations desde banco vazio.</what-built>
  <how-to-verify>1. PostgreSQL e Redis reais respondem pelas URLs de teste. 2. Migrations aplicam do zero. 3. O limiter Redis concreto responde. 4. Preflight retorna zero. 5. SUMMARY registra somente origem redigida e resultado; declara explicitamente que CI final permanece pendente no 02-24.</how-to-verify>
  <resume-signal>Forneça URLs de teste reais e aprove somente quando o preflight inicial terminar verde.</resume-signal>
  <verify><automated>rtk pnpm phase2:integration:preflight</automated></verify>
  <done>PostgreSQL/Redis reais e migrations terminaram verdes; CI final permanece não aprovada até 02-24.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| executor→test infrastructure | URLs reais controlam o preflight inicial de PostgreSQL/Redis. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-37 | Tampering | skipped preflight inicial | mitigate | Checkpoint non-autonomous; somente serviços/migrations reais desbloqueiam dependentes. |
| T-02-38 | Information Disclosure | test URLs/logs | mitigate | Secrets via env e SUMMARY redigido. |
| T-02-SC | Tampering | serviços de teste | mitigate | URLs redigidas e preflight fail-closed; CI final é isolada no 02-24. |
</threat_model>

<verification>Checkpoint aprovado apenas com PostgreSQL, Redis e migrations reais verdes; não aprova CI.</verification>
<success_criteria>02-04 e 02-05 dependem explicitamente deste gate e não executam antecipadamente.</success_criteria>
<output>Create `.planning/phases/02-identidade-organizacoes-e-autorizacao/02-17-SUMMARY.md` when done.</output>
