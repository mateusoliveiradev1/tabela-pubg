---
phase: 02-identidade-organizacoes-e-autorizacao
plan: 38
type: execute
wave: 9
depends_on: [02-32]
files_modified:
  - packages/contracts/src/platform-route-inventory.ts
  - packages/contracts/src/platform-route-inventory.spec.ts
  - packages/contracts/src/index.ts
  - apps/api/src/app.module.spec.ts
  - apps/web/src/app/api/platform/[...path]/route.ts
  - apps/web/src/app/api/platform/[...path]/route.spec.ts
autonomous: true
gap_closure: true
requirements: [AUTH-003, AUTH-005, ORG-002, ORG-003, NFR-005]
must_haves:
  truths:
    - "Cada combinação method/path allowlisted pelo BFF corresponde a uma rota realmente registrada pelo Nest."
    - "Toda rota tenant declara extractor server-side; nenhuma rota sintética de teste existe no inventário de produção."
  artifacts:
    - path: "packages/contracts/src/platform-route-inventory.ts"
      provides: "Manifesto executável BFF→Nest"
    - path: "apps/api/src/app.module.spec.ts"
      provides: "Contract test contra Fastify/Nest real registrado"
  key_links:
    - from: "apps/web/src/app/api/platform/[...path]/route.ts"
      to: "packages/contracts/src/platform-route-inventory.ts"
      via: "BFF rules compiled from canonical manifest"
      pattern: "PlatformRouteInventory"
    - from: "apps/api/src/app.module.spec.ts"
      to: "Fastify hasRoute"
      via: "all manifest methods and concrete sample paths"
      pattern: "hasRoute"
---

<objective>
Contratar o inventário BFF contra o router Nest realmente composto.
Purpose: impedir que UI/BFF anuncie novamente uma rota ausente ou um tenant extractor incompleto.
Output: manifesto canônico, BFF compilado e teste de registro Fastify/Nest.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-32-SUMMARY.md
@apps/web/src/app/api/platform/[...path]/route.ts
@apps/api/src/app.module.spec.ts
@apps/api/src/identity/session.controller.ts
@apps/api/src/identity/identity-management.controller.ts

<interfaces>
The manifest records method/path template, concrete sample path, canonical upstream Nest path, body limit, referrer/CSRF behavior and optional tenant/scope extractor.
Every dynamic UUID segment has an explicit example; tenant authority comes only from the matched route.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Contratar todo inventário BFF contra o router Nest real</name>
  <files>packages/contracts/src/platform-route-inventory.ts, packages/contracts/src/platform-route-inventory.spec.ts, packages/contracts/src/index.ts, apps/api/src/app.module.spec.ts, apps/web/src/app/api/platform/[...path]/route.ts, apps/web/src/app/api/platform/[...path]/route.spec.ts</files>
  <read_first>
    - apps/web/src/app/api/platform/[...path]/route.ts
    - apps/web/src/app/api/platform/[...path]/route.spec.ts
    - apps/api/src/app.module.spec.ts
    - apps/api/src/app.module.ts
    - apps/api/src/identity/session.controller.ts
    - apps/api/src/identity/identity-management.controller.ts
    - apps/api/src/organizations/organizations.controller.ts
    - apps/api/src/organizations/invitations.controller.ts
    - apps/api/src/organizations/members.controller.ts
    - apps/api/src/audit/audit.controller.ts
    - .planning/phases/02-identidade-organizacoes-e-autorizacao/02-CONTEXT.md (D-09, D-15)
  </read_first>
  <behavior>
    - Manifest enumerates every BFF method/path template, upstream Nest template, body limit, referrer/CSRF rotation and tenant extractor.
    - BFF rejects any route absent from manifest and drops browser tenant/scope headers before applying route-derived context.
    - Contract test boots the actual composed Nest application and asserts `hasRoute` for every manifest method with a concrete sample URL.
  </behavior>
  <action>Extract the declarative route inventory into `@pubg-camp/contracts` and have the BFF compile its matchers from it. Include exact example paths for dynamic UUID segments and the canonical upstream path. Remove the unconditional synthetic `__e2e/logos` BFF route; 02-33 provides a separate second-origin test image server. Extend `app.module.spec.ts` to boot the actual composed Nest module, iterate every manifest method/path and call Fastify `hasRoute`; also assert no duplicate method/path rules and every tenant route declares its extractor. Inspect real Nest registration, not source text or a mocked route list. Preserve the Phase 2.1 visual scope fence: this plan changes routing contracts only.</action>
  <acceptance_criteria>
    - Every current BFF route passes against concrete Nest registration, including all session/identity routes from 02-32.
    - Deleting a controller method or adding a BFF-only rule makes the contract test fail.
    - Production manifest contains no synthetic handler, and tenant paths derive canonical UUID context server-side.
  </acceptance_criteria>
  <verify><automated>rtk pnpm --filter @pubg-camp/contracts exec vitest run src/platform-route-inventory.spec.ts &amp;&amp; rtk pnpm --filter @pubg-camp/api exec vitest run src/app.module.spec.ts &amp;&amp; rtk pnpm --filter @pubg-camp/web exec vitest run "src/app/api/platform/[...path]/route.spec.ts"</automated></verify>
  <cleanup>Fastify/Nest app is closed in `finally`; route inventory tests perform no network calls and restore env/fetch stubs.</cleanup>
  <done>Every route advertised by the BFF is proven against the real Nest router registration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| browser→BFF manifest | Only exact method/path matches may be forwarded. |
| BFF manifest→Nest router | A declared path must exist with the same method upstream. |

## ASVS / STRIDE Threat Register
| Threat ID | ASVS | STRIDE | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-85 | V4/V15 | Tampering/DoS | BFF phantom route | mitigate | Canonical manifest + Fastify real-registration contract test. |
| T-02-97 | V8 | Elevation of Privilege | tenant extractor | mitigate | Mandatory route-derived extractor and browser-header stripping. |
</threat_model>

<verification>
- `rtk pnpm --filter @pubg-camp/contracts exec vitest run src/platform-route-inventory.spec.ts`
- `rtk pnpm --filter @pubg-camp/api exec vitest run src/app.module.spec.ts`
- `rtk pnpm --filter @pubg-camp/web exec vitest run "src/app/api/platform/[...path]/route.spec.ts"`
</verification>

<success_criteria>O inventário canônico impede rotas fantasmas e mantém contexto tenant server-derived sem antecipar UI da Fase 2.1.</success_criteria>
<output>Create `.planning/phases/02-identidade-organizacoes-e-autorizacao/02-38-SUMMARY.md` when done.</output>
