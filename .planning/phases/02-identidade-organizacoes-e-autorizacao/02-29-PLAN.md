---
phase: 02-identidade-organizacoes-e-autorizacao
plan: 29
type: execute
wave: 4
depends_on: [02-35]
files_modified:
  - apps/web/src/app/api/platform/[...path]/route.ts
  - apps/web/src/app/api/platform/[...path]/route.spec.ts
  - apps/api/src/authorization/permission.guard.ts
  - apps/api/src/authorization/permission.guard.spec.ts
  - apps/api/src/authorization/authorization.module.ts
  - apps/api/src/main.ts
  - apps/api/test/security.integration.spec.ts
autonomous: true
gap_closure: true
requirements: [AUTH-005, ORG-002, ORG-003, NFR-005]
must_haves:
  truths:
    - "BFF deriva organization/scope somente do route contract já validado e ignora qualquer autoridade tenant enviada pelo navegador."
    - "Nest permite same-tenant, nega path/header/scope divergente sem revelar existência e registra a negação estruturada."
    - "Mudança de membership/role afeta a próxima request sem alterar acesso do ator às outras organizações."
  artifacts:
    - path: "apps/web/src/app/api/platform/[...path]/route.ts"
      provides: "Route rules com tenant/scope extractor server-side"
    - path: "apps/api/src/authorization/permission.guard.ts"
      provides: "Path/header consistency, default-deny e denial recorder"
  key_links:
    - from: "apps/web/src/app/api/platform/[...path]/route.ts"
      to: "apps/api/src/authorization/permission.guard.ts"
      via: "internal x-organization-id/x-authorization-scope-id derived from matched route"
      pattern: "x-organization-id"
    - from: "apps/api/src/authorization/permission.guard.ts"
      to: "authorization snapshot PostgreSQL"
      via: "validated route tenant and current actor"
      pattern: "authorization.can"
---

<objective>
Restaurar o caminho same-tenant e tornar tentativas cross-tenant negadas e registradas.
Purpose: cumprir D-15 e o critério do ROADMAP sem aceitar autoridade tenant controlada pelo browser.
Output: extractor BFF, guard Nest consistente, security audit e integração negativa.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-35-SUMMARY.md
@apps/web/src/app/api/platform/[...path]/route.ts
@apps/api/src/authorization/permission.guard.ts
@apps/api/src/organizations/members.controller.ts
@apps/api/src/organizations/invitations.controller.ts

<interfaces>
Organization routes carry `:organizationId` in the validated path; future scoped rules must declare an explicit scope extractor.
Browser request headers never supply `x-organization-id` or `x-authorization-scope-id` authority.
Authorization denial recorder receives stable category, correlationId, actorId, sessionId, route tenant/scope and permission; never cookies/body/resource details.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derivar tenant/scope na regra BFF validada</name>
  <files>apps/web/src/app/api/platform/[...path]/route.ts, apps/web/src/app/api/platform/[...path]/route.spec.ts</files>
  <read_first>
    - apps/web/src/app/api/platform/[...path]/route.ts
    - apps/web/src/app/api/platform/[...path]/route.spec.ts
    - apps/api/src/organizations/members.controller.ts
    - apps/api/src/organizations/invitations.controller.ts
    - apps/api/src/audit/audit.controller.ts
  </read_first>
  <behavior>
    - Each organization rule extracts the UUID captured by its already matched regex and sets the internal organization header.
    - Incoming browser organization/scope headers are dropped even if supplied; route-derived values overwrite any forwarded value.
    - Root identity/invitation routes carry no fabricated tenant; malformed/mismatched route never reaches fetch.
  </behavior>
  <action>Extend `RouteRule` with explicit named tenant/scope extraction from the matched validated path, not body/query/cookie. Use regex captures or a canonical match function that returns both rule and context; after forwarding the allowlisted headers, set internal `x-organization-id` and optional scope. Keep tenant headers absent from the browser request allowlist. Reject a rule whose required extractor cannot return a canonical UUID. Per D-09/D-15, do not persist an active organization in session or cookie.</action>
  <acceptance_criteria>
    - Tests inspect the exact upstream fetch headers for members, invitations, ownership, audit and logo routes.
    - Spoofed browser headers cannot replace route-derived context.
    - Non-tenant identity routes and invalid route/method combinations preserve current behavior.
  </acceptance_criteria>
  <verify><automated>rtk pnpm --filter @pubg-camp/web exec vitest run "src/app/api/platform/[...path]/route.spec.ts" &amp;&amp; rtk pnpm --filter @pubg-camp/web typecheck</automated></verify>
  <cleanup>Fetch/environment stubs are restored after each test; no real upstream mutation is issued.</cleanup>
  <done>Every tenant BFF request carries server-derived context and no browser-controlled tenant authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validar contexto no Nest e registrar cross-tenant deny</name>
  <files>apps/api/src/authorization/permission.guard.ts, apps/api/src/authorization/permission.guard.spec.ts, apps/api/src/authorization/authorization.module.ts, apps/api/src/main.ts, apps/api/test/security.integration.spec.ts</files>
  <read_first>
    - apps/api/src/authorization/permission.guard.ts
    - apps/api/src/authorization/authorization.module.ts
    - apps/api/src/authorization/authorization.service.ts
    - apps/api/src/main.ts
    - apps/api/test/security.integration.spec.ts
    - .planning/phases/02-identidade-organizacoes-e-autorizacao/02-CONTEXT.md (D-13, D-14, D-15, D-17)
  </read_first>
  <behavior>
    - Same actor + route organization + matching internal header + live permission allows.
    - Header absent, path/header mismatch, wrong-org membership, wrong-scope ownership and stale role deny uniformly before the controller mutation.
    - Every cross-tenant denial emits exactly one redacted structured security record; response remains uniform 403/support code.
  </behavior>
  <action>Resolve `organizationId` and optional scope from Nest route params and compare them to the BFF internal headers before calling `AuthorizationService`; direct callers cannot authorize org A while invoking a path for org B. Add an injected `AuthorizationDenialRecorder` port in `AuthorizationModule`, wired to structured security logging in `main.ts`. Record a stable reason class and correlation id without request body, resource existence, roles, cookies or secrets. Per research and D-17, keep denied attempts in the security audit channel rather than user-visible business `audit_events`, which require tenant membership and must not be attacker-inflatable.</action>
  <acceptance_criteria>
    - Fastify/Nest integration proves same-tenant 2xx/expected domain result and cross-tenant 403 with controller/repository mutation spy untouched.
    - Captured denial record is exactly once, redacted and correlated; missing recorder cannot turn deny into allow.
    - Current live PostgreSQL snapshot still decides roles; BFF never becomes RBAC authority.
  </acceptance_criteria>
  <verify><automated>rtk pnpm --filter @pubg-camp/api exec vitest run src/authorization/permission.guard.spec.ts test/security.integration.spec.ts &amp;&amp; rtk pnpm --filter @pubg-camp/authorization exec vitest run src/authorization.spec.ts</automated></verify>
  <cleanup>Nest test app, database fakes and log capture are closed/reset; integration uses run-scoped tenant rows removed in `finally`.</cleanup>
  <done>Authorized same-tenant collaboration works; cross-tenant attempts are denied and recorded without disclosure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| browser→Next BFF | Path is untrusted input; tenant headers are never authority. |
| BFF→Nest | Internal context must match Nest route params and current session. |
| Nest→security audit | Denial evidence must be useful without leaking or becoming business audit spam. |

## ASVS / STRIDE Threat Register
| Threat ID | ASVS | STRIDE | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-71 | V8 | Elevation of Privilege | tenant header spoofing | mitigate | Route-derived header plus Nest path/header equality. |
| T-02-72 | V8/V14 | Information Disclosure | cross-tenant BOLA | mitigate | Uniform deny before controller/repository; no existence detail. |
| T-02-73 | V16 | Repudiation | denied attempt | mitigate | One correlated structured redacted security record. |
| T-02-74 | V2 | DoS | denial log flooding | mitigate | Stable bounded fields; existing perimeter rate limit; no attacker body. |
</threat_model>

<verification>
- `rtk pnpm --filter @pubg-camp/web exec vitest run "src/app/api/platform/[...path]/route.spec.ts"`
- `rtk pnpm --filter @pubg-camp/api exec vitest run src/authorization/permission.guard.spec.ts test/security.integration.spec.ts`
- `rtk pnpm --filter @pubg-camp/authorization exec vitest run src/authorization.spec.ts`
</verification>

<success_criteria>Same-tenant requests reach authorized handlers and cross-tenant requests are uniformly denied plus recorded.</success_criteria>
<output>Create `.planning/phases/02-identidade-organizacoes-e-autorizacao/02-29-SUMMARY.md` when done.</output>
