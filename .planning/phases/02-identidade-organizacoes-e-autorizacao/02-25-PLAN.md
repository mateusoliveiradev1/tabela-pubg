---
phase: 02-identidade-organizacoes-e-autorizacao
plan: 25
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/database/src/schema.ts
  - packages/database/src/schema/identity.ts
  - packages/database/src/outbox.ts
  - packages/database/src/outbox.test.ts
  - packages/database/migrations/0003_runtime_security_closure.sql
  - packages/database/migrations/meta/0003_snapshot.json
  - packages/database/migrations/meta/_journal.json
  - packages/database/test/runtime-security-migration.integration.spec.ts
autonomous: true
gap_closure: true
requirements: [AUTH-002, AUTH-003, ORG-002, NFR-005]
must_haves:
  truths:
    - "Sessão persiste trust, desafios não-sign-in ficam vinculados ao ator/sessão e provas de vínculo/OAuth são duráveis e consumíveis uma vez."
    - "Outbox possui claim com lease, retry e terminal published sem permitir que dois publishers possuam o mesmo evento simultaneamente."
    - "A migração aplica do zero e sobre o schema anterior em PostgreSQL real, com backfill fail-closed e cleanup isolado."
  artifacts:
    - path: "packages/database/migrations/0003_runtime_security_closure.sql"
      provides: "Evolução cumulativa de trust, auth proof e lease do outbox"
    - path: "packages/database/src/outbox.ts"
      provides: "Estado persistente claim/retry/published do publisher"
    - path: "packages/database/test/runtime-security-migration.integration.spec.ts"
      provides: "Gate PostgreSQL real para upgrade e fresh schema"
  key_links:
    - from: "packages/database/src/schema.ts"
      to: "packages/database/src/outbox.ts"
      via: "claim token + lease expiry + status condicional"
      pattern: "claimToken|leaseExpiresAt"
    - from: "packages/database/src/schema/identity.ts"
      to: "packages/database/migrations/0003_runtime_security_closure.sql"
      via: "Drizzle schema e SQL cumulativo equivalentes"
      pattern: "session_trust|identity_link_proofs|current_method_confirmed_at"
---

<objective>
Criar os contratos persistentes irreversíveis que todos os demais gaps de runtime consomem.
Purpose: impedir que sessão, OTP, OAuth e outbox dependam de estado transitório ou ambíguo.
Output: schema Drizzle, migração cumulativa, state machine do outbox e gate PostgreSQL real.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-VERIFICATION.md
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-REVIEW.md
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-CONTEXT.md
@packages/database/src/schema.ts
@packages/database/src/schema/identity.ts
@packages/database/src/outbox.ts

<interfaces>
Existing outbox states: pending | publishing | published | failed.
Existing identity purposes: OTP sign-in | link-email | change-email | step-up; OAuth sign-in | link-identity | step-up.
Downstream contracts require SessionTrust = provisional | trusted and BullMQ jobId = outbox event id.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persistir trust e provas actor/session-bound</name>
  <files>packages/database/src/schema/identity.ts, packages/database/migrations/0003_runtime_security_closure.sql, packages/database/migrations/meta/0003_snapshot.json, packages/database/migrations/meta/_journal.json</files>
  <read_first>
    - packages/database/src/schema/identity.ts
    - packages/database/migrations/0001_identity_organizations.sql
    - packages/database/migrations/0002_organization_logos.sql
    - .planning/phases/02-identidade-organizacoes-e-autorizacao/02-CONTEXT.md (D-02, D-03, D-05, D-06, D-08, D-16)
  </read_first>
  <behavior>
    - Sessions require explicit `provisional|trusted` trust; legacy rows are backfilled trusted only when a live verified email exists, otherwise provisional with the 15-minute cap enforced.
    - Every non-sign-in OTP challenge stores matching userId+sessionId; sign-in stores neither; composite FK proves the session belongs to the actor.
    - OAuth transactions store a timestamped current-method proof bound to userId/sessionId; pending identity-link proofs are actor/session/provider-bound, expiring and one-use.
  </behavior>
  <action>Per D-02/D-03/D-05/D-06/D-08/D-16, add a `session_trust` enum and non-null `sessions.trust`, `auth_challenges.session_id` with purpose/binding checks and composite user/session FK, `oauth_transactions.current_method_confirmed_at`, and an `identity_link_proofs` table carrying only server-side candidate data, actor/session binding, expiry and consumed timestamp. Generate `0003_runtime_security_closure.sql`; do not edit 0000-0002. The upgrade must backfill existing sessions from durable verified-email state instead of assuming full trust; an unverified legacy session becomes provisional and cannot outlive its accepted 15-minute window. Add indexes for active proofs and expiry. Keep raw OTP, OAuth state, browser binding, session token and provider token absent from all new columns.</action>
  <acceptance_criteria>
    - The real-PostgreSQL migration spec asserts column names, enums, FKs, purpose checks, one-use fields and absence of plaintext credential columns.
    - Migration is additive/cumulative, preserves satisfied Discord identities and memberships, and performs deterministic fail-closed backfill.
    - D-03 is enforceable without email coincidence merge; D-08 proofs are bound to the reconfirming session.
  </acceptance_criteria>
  <verify><automated>rtk pnpm --filter @pubg-camp/database test -- schema/identity &amp;&amp; rtk pnpm verify:migrations</automated></verify>
  <cleanup>Generation may replace only the new 0003 migration/snapshot before commit; never drop, rewrite or reset an existing shared schema.</cleanup>
  <done>Identity/session security state is represented durably with schema and migration parity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar state machine durável do outbox</name>
  <files>packages/database/src/schema.ts, packages/database/src/outbox.ts, packages/database/src/outbox.test.ts, packages/database/migrations/0003_runtime_security_closure.sql, packages/database/migrations/meta/0003_snapshot.json</files>
  <read_first>
    - packages/database/src/schema.ts
    - packages/database/src/outbox.ts
    - packages/database/src/outbox.test.ts
    - packages/database/src/repositories/notifications.ts
    - packages/database/src/repositories/organizations.ts (storage.logo.cleanup event)
  </read_first>
  <behavior>
    - Claim uses `FOR UPDATE SKIP LOCKED`, moves eligible pending/failed or expired-lease publishing rows to publishing, increments attempts and assigns a unique claim token plus lease expiry.
    - Mark-published and retry require event id + current claim token; stale owners cannot change state.
    - Retry stores a redacted bounded error code and next availableAt; published is terminal with publishedAt and cleared lease fields.
  </behavior>
  <action>Add nullable `claim_token` and `lease_expires_at` plus the exact pending/lease index to `outbox_events`. Export `claimOutboxBatch`, `markOutboxPublished` and `retryOutboxEvent` from `outbox.ts`; accept injected clock/claim token and use conditional updates so lease ownership is explicit. Maximum attempts transitions to failed with a bounded next retry policy; do not delete evidence or store provider/recipient/token data in `last_error`. Preserve `appendOutboxEvent` and existing event payloads.</action>
  <acceptance_criteria>
    - Two concurrent claims cannot return the same event while a live lease exists.
    - An expired publishing lease is reclaimable; stale claim token cannot publish or retry it.
    - Published rows have status/publishedAt and cannot be reclaimed; retry retains only stable redacted error text.
  </acceptance_criteria>
  <verify><automated>rtk pnpm --filter @pubg-camp/database test -- outbox</automated></verify>
  <cleanup>Unit tests use in-memory fakes only; no database rows or queue jobs survive the task.</cleanup>
  <done>PostgreSQL exposes a lease-safe, retryable outbox publication contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Executar gate de migration push em PostgreSQL isolado</name>
  <files>packages/database/test/runtime-security-migration.integration.spec.ts</files>
  <read_first>
    - scripts/run-phase2-integration.mjs
    - vitest.integration.config.ts
    - packages/database/test/identity-organizations.integration.spec.ts
    - packages/database/migrations/0003_runtime_security_closure.sql
  </read_first>
  <behavior>
    - Fresh apply 0000→0003 creates all new constraints and indexes.
    - Upgrade 0000→0002 seed→0003 preserves users/identities/memberships and backfills trust safely.
    - Invalid actor/session binding, double proof consumption and stale outbox lease mutation fail in real PostgreSQL.
  </behavior>
  <action>Create a PostgreSQL integration spec that provisions a unique run-scoped schema, applies migrations from zero and via the explicit pre-0003 upgrade path, asserts `information_schema`/catalog constraints and executes the negative cases. Treat missing PostgreSQL as failure. This is the schema/migration push gate for all dependent plans: do not use `drizzle-kit push` against a shared database and do not permit downstream execution until it passes.</action>
  <acceptance_criteria>
    - Both migration paths pass against PostgreSQL, not PGlite/mock/SQL text inspection.
    - Existing identity and organization rows survive upgrade with correct trust classification.
    - Test fails closed on missing DATABASE_URL, migration mismatch or leaked schema.
  </acceptance_criteria>
  <verify><automated>rtk pnpm phase2:integration</automated></verify>
  <cleanup>Use a cryptographically unique schema name and `DROP SCHEMA ... CASCADE` in `finally`; validate the resolved name prefix before dropping and never target public or a caller-owned schema.</cleanup>
  <done>Migration 0003 is proven from zero and as an upgrade on real PostgreSQL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| API/worker→PostgreSQL | Security state and queue publication ownership become durable. |
| migration runner→database | Schema evolution can corrupt or over-trust existing sessions. |

## ASVS / STRIDE Threat Register
| Threat ID | ASVS | STRIDE | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-56 | V7 | Elevation of Privilege | session trust backfill | mitigate | Verified-email-derived backfill; unverified sessions provisional/capped. |
| T-02-57 | V6/V10 | Spoofing/Tampering | OTP/OAuth/link proofs | mitigate | Composite actor/session binding, expiry and conditional one-use consume. |
| T-02-58 | V15 | Tampering/DoS | outbox claim | mitigate | SKIP LOCKED lease ownership, bounded retry and terminal published state. |
| T-02-59 | V13/V15 | Tampering | migration | mitigate | Fresh+upgrade PostgreSQL gate in isolated schema; no shared db push. |
</threat_model>

<verification>
- `rtk pnpm verify:migrations`
- `rtk pnpm --filter @pubg-camp/database test`
- `rtk pnpm phase2:integration`
</verification>

<success_criteria>Todos os demais planos podem depender de contratos persistentes explícitos, migráveis e fail-closed.</success_criteria>
<output>Create `.planning/phases/02-identidade-organizacoes-e-autorizacao/02-25-SUMMARY.md` when done.</output>
