---
phase: 02-identidade-organizacoes-e-autorizacao
plan: 15
type: execute
wave: 16
depends_on: [02-20]
files_modified: []
autonomous: false
requirements: [AUTH-001, AUTH-002, AUTH-003, AUTH-006, NFR-005]
user_setup:
  - service: discord
    why: "Smoke real do redirect, callback, scopes e PKCE"
    env_vars:
      - name: DISCORD_CLIENT_ID
        source: "Discord Developer Portal"
      - name: DISCORD_CLIENT_SECRET
        source: "Discord Developer Portal"
      - name: DISCORD_REDIRECT_URI
        source: "Redirect sandbox exato"
  - service: resend
    why: "Smoke de entrega real de OTP e alerta de novo dispositivo"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard"
      - name: EMAIL_FROM
        source: "Domínio remetente verificado"
must_haves:
  truths:
    - "OAuth Discord sandbox confirma state, callback e suporte PKCE S256 ou registra exceção comprovada."
    - "OTP/novo dispositivo chegam sem segredo em URL/log e são single-use."
    - "Cookie `__Host-session` está Secure atrás de TLS e HTTP/origem externa são rejeitados."
    - "Leitor de tela, teclado, zoom 200%, contraste AA e reduced motion são aprovados manualmente após o E2E."
  artifacts: []
  key_links: []
---

<objective>
Validar as três dependências que somente um ambiente externo real pode provar.
Purpose: concluir AUTH/NFR sem afirmar Discord, e-mail ou TLS simulados.
Output: checkpoint humano com evidência sandbox e decisão PKCE registrada.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-VALIDATION.md
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-RESEARCH.md
</context>

<tasks>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verificar Discord OAuth e gate PKCE no sandbox</name>
  <files>Nenhum arquivo é modificado neste checkpoint.</files>
  <action>Usar as credenciais sandbox configuradas para executar o fluxo real já automatizado, coletar evidência sem segredo e aguardar o parecer humano; não alterar o fallback PKCE sem evidência de recusa do Discord.</action>
  <what-built>Authorization Code server-side com state/browser binding, revogação de token e PKCE S256 required.</what-built>
  <how-to-verify>1. Entrar e vincular com app Discord sandbox. 2. Confirmar redirect exato, scopes identify/email, state mismatch/replay negados e nenhuma credencial no browser/log. 3. Confirmar PKCE S256 aceito. Se Discord recusar comprovadamente os parâmetros, registrar resposta/evidência e habilitar somente o fallback explícito documentado; state/browser binding permanece obrigatório.</how-to-verify>
  <resume-signal>Digite "discord aprovado" ou descreva a falha e evidência PKCE.</resume-signal>
  <verify><automated>rtk pnpm --filter @pubg-camp/api test -- identity/oauth security</automated></verify>
  <done>Fluxo Discord real foi aprovado e o resultado do gate PKCE foi registrado sem credenciais.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificar leitor de tela, zoom, contraste e motion</name>
  <files>Nenhum arquivo é modificado neste checkpoint.</files>
  <action>Abrir o build aprovado em navegador real e executar a inspeção assistiva manual complementar ao Playwright/axe, registrando navegador, leitor e viewport sem alterar código neste checkpoint.</action>
  <what-built>Todas as telas privadas e públicas da fase, com testes browser-level automatizados em três viewports.</what-built>
  <how-to-verify>1. Navegar login, OTP, onboarding, sessões, membros, convites, step-up e auditoria apenas por teclado. 2. Repetir fluxo crítico com NVDA ou VoiceOver e confirmar headings, labels, live regions, dialogs e focus return. 3. Aplicar zoom 200% em 320px/768px/1440px e confirmar ausência de perda/scroll horizontal. 4. Conferir contraste AA com ferramenta do navegador e `prefers-reduced-motion`, inclusive destructive/disabled/focus states.</how-to-verify>
  <resume-signal>Digite "acessibilidade aprovada" ou descreva tela, viewport e falha.</resume-signal>
  <verify><automated>rtk pnpm phase2:integration:preflight &amp;&amp; rtk pnpm --filter @pubg-camp/web test:e2e:smoke</automated></verify>
  <done>Keyboard, screen reader, zoom 200%, contraste e reduced motion foram aprovados manualmente.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificar entrega real de e-mail e cookie TLS</name>
  <files>Nenhum arquivo é modificado neste checkpoint.</files>
  <action>Executar os smokes externos de Resend e HTTPS com dados de teste, inspecionar headers/URL/logs e aguardar aprovação humana; nenhum resultado externo deve ser inferido pelos testes fake.</action>
  <what-built>OTP, convite/novo-device via worker idempotente e cookie opaco `__Host-session`.</what-built>
  <how-to-verify>1. Solicitar/consumir OTP e confirmar expiração, uso único e resposta uniforme. 2. Criar convite real, conferir entrega, acionar retry do mesmo delivery sem duplicata, reenviar e confirmar nova entrega/link enquanto o anterior fica inválido; aceitar e conferir papéis globais/operacionais. 3. Entrar em novo dispositivo, validar contexto read-only nos estados active/already-revoked/not-found e revogar somente por POST confirmado. 4. Por HTTPS, inspecionar Secure/HttpOnly/SameSite=Lax/Path=/ sem Domain; confirmar POST sem CSRF/origem externa e HTTP rejeitados. 5. Buscar code/token/cookie em URL, HTML e logs: nenhum pode existir.</how-to-verify>
  <resume-signal>Digite "fase 2 aprovada" ou relate o comportamento divergente.</resume-signal>
  <verify><automated>rtk pnpm phase2:integration:preflight &amp;&amp; rtk pnpm --filter @pubg-camp/worker test -- notifications &amp;&amp; rtk pnpm --filter @pubg-camp/api test -- security.integration</automated></verify>
  <done>Entrega real, CTA read-only, sessão revogável e cookie TLS foram aprovados com evidência.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| produção sandbox→serviços externos | Redirect, entrega e TLS só são prováveis fora dos fakes. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-32 | Spoofing | real OAuth/TLS | mitigate | Sandbox + inspeção browser/log e fallback PKCE documentado. |
| T-02-33 | Information Disclosure | real email/browser | mitigate | Verificação de URL/log/headers e CTA GET read-only. |
| T-02-SC | Tampering | external services | transfer | Provider/TLS são terceiros; adapters e evidência sandbox limitam risco. |
</threat_model>

<verification>Os três checkpoints foram aprovados com evidência; nenhum resultado externo foi inferido.</verification>
<success_criteria>Os três checkpoints — Discord/PKCE, acessibilidade manual e Resend/TLS — foram aprovados; integrações reais e experiência assistiva cumprem os contratos.</success_criteria>
<output>Create `.planning/phases/02-identidade-organizacoes-e-autorizacao/02-15-SUMMARY.md` when done.</output>
