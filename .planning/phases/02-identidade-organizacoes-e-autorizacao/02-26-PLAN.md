---
phase: 02-identidade-organizacoes-e-autorizacao
plan: 26
type: execute
wave: 2
depends_on: [02-25]
files_modified:
  - apps/worker/src/outbox/publisher.ts
  - apps/worker/src/outbox/publisher.spec.ts
  - apps/worker/src/main.ts
  - apps/worker/src/notifications/processor.ts
  - apps/worker/src/notifications/processor.spec.ts
autonomous: true
gap_closure: true
requirements: [AUTH-002, ORG-002, NFR-005]
must_haves:
  truths:
    - "Eventos pending são reivindicados, validados e publicados na fila correta com `jobId = outbox.id`."
    - "Falha PostgreSQL/Redis/BullMQ mantém retry durável; replay nunca cria um segundo efeito lógico."
    - "OTP, convite, alerta e logo cleanup chegam aos processors contendo somente deliveryId ou cleanupId."
  artifacts:
    - path: "apps/worker/src/outbox/publisher.ts"
      provides: "Loop PostgreSQL→BullMQ lease-safe"
    - path: "apps/worker/src/main.ts"
      provides: "Lifecycle conjunto de publisher, producers e consumers"
  key_links:
    - from: "apps/worker/src/outbox/publisher.ts"
      to: "outbox-delivery | storage-logo-cleanup"
      via: "strict event mapper + queue.add(..., { jobId: event.id })"
      pattern: "jobId.*event.id"
    - from: "apps/worker/src/main.ts"
      to: "packages/database/src/outbox.ts"
      via: "claim/retry/published ports"
      pattern: "claimOutboxBatch|markOutboxPublished|retryOutboxEvent"
---

<objective>
Conectar de forma durável o outbox transacional às filas e processors BullMQ existentes.
Purpose: fazer e-mails e limpezas confirmados no PostgreSQL realmente chegarem ao worker sem duplicidade nem vazamento.
Output: publisher, mapeamento estrito, lifecycle e semântica terminal robusta.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-25-SUMMARY.md
@apps/worker/src/main.ts
@apps/worker/src/notifications/processor.ts
@apps/worker/src/storage/logo-cleanup.processor.ts
@packages/queue/src/index.ts

<interfaces>
`notification.delivery.requested` payload is exactly `{ deliveryId: uuid }` and maps to queue `outbox-delivery`.
`storage.logo.cleanup` payload is exactly `{ cleanupId: uuid }` and maps to queue `storage-logo-cleanup`.
Every BullMQ job uses the outbox event UUID as `jobId`; queue data never includes recipient, object key, token or ciphertext.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Publicar claims do outbox com job id idempotente</name>
  <files>apps/worker/src/outbox/publisher.ts, apps/worker/src/outbox/publisher.spec.ts</files>
  <read_first>
    - packages/database/src/outbox.ts
    - packages/database/src/schema.ts
    - packages/queue/src/index.ts
    - packages/database/src/repositories/notifications.ts
    - packages/database/src/repositories/organizations.ts (storage.logo.cleanup)
  </read_first>
  <behavior>
    - A claimed notification event enqueues only `{deliveryId}` on `outbox-delivery` with `jobId=event.id`, then conditionally marks published.
    - A claimed cleanup event enqueues only `{cleanupId}` on `storage-logo-cleanup` with the same jobId rule.
    - Unknown event/version, invalid payload, enqueue failure or lost lease persists a bounded retry/failure result and never marks published falsely.
  </behavior>
  <action>Implement an injected `OutboxPublisher` with `runOnce()` and abortable polling lifecycle. Validate event type, version 1 and strict UUID-only payload before queue access. Claim a bounded batch, call the correct injected BullMQ producer with `jobId: event.id`, and use the claim token for published/retry transitions. BullMQ duplicate-job success is treated as idempotent publication only after confirming the queue call did not fail. Use stable error categories; never copy thrown provider messages or payloads into logs/database.</action>
  <acceptance_criteria>
    - Unit tests cover both mappings, strict rejection, duplicate job replay, enqueue failure, lost lease and partial batch continuation.
    - No log/job/error contains recipient, OTP, invite token, alert context, object key or ciphertext.
    - Publisher can be stopped without claiming new work and drains only its current bounded batch.
  </acceptance_criteria>
  <verify><automated>rtk pnpm --filter @pubg-camp/worker test -- outbox/publisher</automated></verify>
  <cleanup>Fake queues and clocks are closed/reset after each test; no timers, Redis handles or jobs remain.</cleanup>
  <done>Outbox claims map to idempotent opaque BullMQ jobs with durable publication outcome.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compor publisher e endurecer terminalidade do delivery</name>
  <files>apps/worker/src/main.ts, apps/worker/src/notifications/processor.ts, apps/worker/src/notifications/processor.spec.ts</files>
  <read_first>
    - apps/worker/src/main.ts
    - apps/worker/src/notifications/processor.ts
    - apps/worker/src/notifications/processor.spec.ts
    - apps/worker/src/storage/logo-cleanup.processor.ts
    - apps/worker/src/outbox/publisher.ts
  </read_first>
  <behavior>
    - Worker starts queue producers, consumers and publisher only after PostgreSQL+Redis readiness; shutdown stops publisher before queues/connections.
    - Delivery returns delivered/expired only when `clear()` persists; false reloads terminal state or throws for BullMQ retry.
    - Publisher retry and consumer retry preserve the delivery idempotency key and do not duplicate provider effects.
  </behavior>
  <action>Create both BullMQ queues through `createQueue`, inject their `add` operations into the publisher, start the publisher after consumers are ready, and close in order: publisher→queues→workers→server→database/Redis/storage/telemetry. Capture shutdown failures without exposing connection strings. Fix WR-01 by requiring conditional payload clear success; on false, reload and return ignored only when the same terminal state already exists, otherwise throw. Keep D-07/D-10 opaque notification payload custody intact.</action>
  <acceptance_criteria>
    - Startup cannot report ready while publisher dependencies are unavailable.
    - SIGINT/SIGTERM closes publisher and both producers/consumers exactly once.
    - A failed terminal clear cannot report delivered/expired, and a concurrent winner is recognized idempotently.
  </acceptance_criteria>
  <verify><automated>rtk pnpm --filter @pubg-camp/worker test &amp;&amp; rtk pnpm --filter @pubg-camp/worker typecheck</automated></verify>
  <cleanup>Tests close fake resources; production shutdown never deletes pending outbox rows or force-clears ciphertext without a persisted terminal outcome.</cleanup>
  <done>Worker owns the complete publisher/consumer lifecycle and reports terminal state truthfully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| PostgreSQL outbox→worker | Durable event becomes process-owned work through a lease. |
| worker→Redis/BullMQ | Only opaque IDs may cross into queue storage. |
| processor→external provider/storage | Retry must not duplicate or leak sensitive payload. |

## ASVS / STRIDE Threat Register
| Threat ID | ASVS | STRIDE | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-60 | V14 | Information Disclosure | job/log payload | mitigate | Strict UUID-only schemas and stable redacted errors. |
| T-02-61 | V15 | Tampering/Repudiation | claim→published transition | mitigate | Claim-token conditional state change and jobId=outbox id. |
| T-02-62 | V2/V15 | DoS | retry loop | mitigate | Bounded batches, exponential availableAt and abortable polling. |
| T-02-63 | V16 | Repudiation | terminal clear | mitigate | Persisted terminal outcome required before success response. |
</threat_model>

<verification>
- `rtk pnpm --filter @pubg-camp/worker test`
- `rtk pnpm --filter @pubg-camp/worker build`
</verification>

<success_criteria>OTP, convite, alerta e cleanup possuem um caminho de produção durável do commit até o processor.</success_criteria>
<output>Create `.planning/phases/02-identidade-organizacoes-e-autorizacao/02-26-SUMMARY.md` when done.</output>
