---
phase: 02-identidade-organizacoes-e-autorizacao
plan: 22
type: execute
wave: 10
depends_on: [02-08, 02-09, 02-21]
files_modified:
  - apps/api/package.json
  - apps/worker/package.json
  - pnpm-lock.yaml
  - apps/api/src/organizations/ports/organization-logo-storage.ts
  - apps/api/src/organizations/adapters/s3-organization-logo-storage.ts
  - apps/api/src/organizations/organizations.service.ts
  - apps/api/src/organizations/organizations.controller.ts
  - apps/api/src/organizations/organizations.module.ts
  - apps/api/src/organizations/organization-logo.integration.spec.ts
  - apps/worker/src/storage/logo-cleanup.processor.ts
  - apps/worker/src/storage/logo-cleanup.processor.spec.ts
  - apps/worker/src/main.ts
autonomous: true
requirements: [AUTH-005, ORG-001, ORG-002, NFR-005]
must_haves:
  truths:
    - "Criar/editar organização recebe multipart no servidor, valida bytes antes do commit e nunca aceita object key do cliente."
    - "Falha no banco apaga o objeto recém-criado; substituição mantém anterior até commit e agenda cleanup durável/idempotente."
    - "API e worker declaram `@pubg-camp/storage: workspace:*`, instalam por lockfile e compilam contra o mesmo adapter."
    - "Somente owner/admin autorizado altera logo; cross-org e MIME inválido não gravam storage/database."
    - "Leitura e preview de convite retornam URL assinada curta ou fallback seguro, nunca bucket/key permanente."
  artifacts:
    - path: "apps/api/src/organizations/organization-logo.integration.spec.ts"
      provides: "Casos multipart, spoof, rollback, autorização e preview"
    - path: "apps/worker/src/storage/logo-cleanup.processor.ts"
      provides: "Delete idempotente/retry do asset substituído"
  key_links:
    - from: "apps/api/src/organizations/organizations.service.ts"
      to: "packages/storage/src/organization-logo.ts"
      via: "port injeta store/delete/download seguro"
      pattern: "OrganizationLogoStorage"
    - from: "apps/api/src/organizations/organizations.service.ts"
      to: "packages/database/src/repositories/organizations.ts"
      via: "upload→transaction activate→cleanup compensatório/outbox"
      pattern: "activateLogo"
    - from: "apps/worker/src/storage/logo-cleanup.processor.ts"
      to: "packages/storage/src/index.ts"
      via: "job cleanupId resolve ledger server-side e chama deleteObject com key validada"
      pattern: "deleteObject"
    - from: "apps/api/package.json + apps/worker/package.json"
      to: "packages/storage/package.json"
      via: "@pubg-camp/storage workspace:* registrado no pnpm-lock"
      pattern: "workspace:*"
---

<objective>
Expor upload/recuperação de logo na API e garantir cleanup durável.
Purpose: ligar a fundação 02-21 à criação/edição da organização e ao preview de convite sem quebrar atomicidade ou autorização.
Output: port/adapter, endpoints/services, integração PostgreSQL/storage e worker de cleanup.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-21-SUMMARY.md
@apps/api/src/organizations/organizations.service.ts
@apps/api/src/health/health.controller.ts
@packages/storage/src/organization-logo.ts
@packages/database/src/repositories/organizations.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Integrar multipart seguro à criação e edição</name>
  <files>apps/api/package.json, pnpm-lock.yaml, apps/api/src/organizations/ports/organization-logo-storage.ts, apps/api/src/organizations/adapters/s3-organization-logo-storage.ts, apps/api/src/organizations/organizations.service.ts, apps/api/src/organizations/organizations.controller.ts, apps/api/src/organizations/organizations.module.ts</files>
  <behavior>
    - POST create com logo válido cria org/owner/asset; logo inválido ou falha DB deixa zero org/asset/object.
    - PUT logo exige owner/admin da mesma org e CSRF; member/cross-org/oversized/spoof falham antes de metadata ativa.
    - GET logo e invitation preview produzem URL assinada curta/fallback; não retornam objectKey/bucket/config.
  </behavior>
  <action>Adicionar `@pubg-camp/storage: workspace:*` ao `apps/api/package.json` e atualizar pnpm-lock. Seguir analogs HealthController/HealthService/OrganizationsModule: controller fino faz parsing multipart streaming com hard limits e passa bytes/claimed MIME, nunca filename/key. Service pré-gera orgId, valida/store via port, executa create/activate em transação e, em erro, tenta delete imediato; se delete falhar, chama o `StorageCleanupRepository.enqueueOrphanCleanup` independente para ledger+outbox por cleanupId. Em replace, só troca ativo no commit e publica cleanup do anterior. Download assinado é gerado sob demanda e o preview de convite só recebe logo depois que o convite opaco foi resolvido read-only; ausência/deleção retorna fallback sem revelar existência externa.</action>
  <verify><automated>rtk pnpm install --frozen-lockfile &amp;&amp; rtk pnpm phase2:integration:preflight &amp;&amp; rtk pnpm --filter @pubg-camp/api test -- organization-logo.integration &amp;&amp; rtk pnpm --filter @pubg-camp/api typecheck &amp;&amp; rtk pnpm --filter @pubg-camp/api build</automated></verify>
  <done>Criação/edição/recuperação usam bytes validados, transação compensada e autorização tenant-aware.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Processar cleanup idempotente e provar falhas</name>
  <files>apps/worker/package.json, pnpm-lock.yaml, apps/api/src/organizations/organization-logo.integration.spec.ts, apps/worker/src/storage/logo-cleanup.processor.ts, apps/worker/src/storage/logo-cleanup.processor.spec.ts, apps/worker/src/main.ts</files>
  <behavior>
    - PutObject success → transação de criação da org falha → delete imediato falha → ledger independente persiste → retry do worker apaga objeto e completa ledger.
    - Worker aceita apenas cleanupId opaco, busca key/provider no ledger server-side, valida schema e trata already-missing como sucesso idempotente.
    - Testes cobrem oversized, MIME spoofed, rollback DB, delete retry, unauthorized/cross-org e URL expirada/missing.
  </behavior>
  <action>Adicionar `@pubg-camp/storage: workspace:*` ao `apps/worker/package.json` e atualizar pnpm-lock. Registrar processor separado; job payload contém somente cleanupId, nunca bytes, URL, key, digest ou organizationId. Worker clama ledger por ID/lease, obtém provider/key exclusivamente do repository, valida ObjectKeySchema, deleta e marca complete; already-missing é sucesso, falha agenda nextAttemptAt/incrementa attempts. Adotar retry/backoff do notification processor 02-08. Teste stateful prova exatamente PutObject success → org transaction failure → immediate delete failure → ledger+outbox independentes persistem → retry cleanup completa, incluindo logs redigidos e rejeição de key controlada pelo usuário.</action>
  <verify><automated>rtk pnpm install --frozen-lockfile &amp;&amp; rtk pnpm phase2:integration:preflight &amp;&amp; rtk pnpm --filter @pubg-camp/api test -- organization-logo.integration &amp;&amp; rtk pnpm --filter @pubg-camp/worker test -- logo-cleanup &amp;&amp; rtk pnpm --filter @pubg-camp/worker typecheck &amp;&amp; rtk pnpm --filter @pubg-camp/worker build</automated></verify>
  <done>Rollback e cleanup são comprovados, retry não duplica delete e autorização impede troca cross-org.</done>
</task>
</tasks>

<execution_notes>
File ownership/commits: Task 1 possui API manifest/port/adapter/service/controller/module e inicia a atualização determinística do lockfile; Task 2 possui worker manifest/spec/processor e finaliza o mesmo lockfile sequencialmente. Criar commits separados (`feat(02-22): upload organization logos securely`, `feat(02-22): cleanup orphan logo objects`) e nunca editar pnpm-lock em paralelo.
</execution_notes>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| browser multipart→API | Bytes, MIME, tamanho e orgId são não confiáveis. |
| DB transaction→object storage | Não existe transação distribuída nativa. |
| outbox→worker storage | Retry pode repetir delete ou receber asset indevido. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-47 | Elevation of Privilege | logo mutation | mitigate | Session/CSRF + capability live + organization explícita. |
| T-02-48 | Tampering | multipart spoof | mitigate | Inspection server-side antes do PutObject. |
| T-02-49 | Repudiation | replace/cleanup | mitigate | Audit/correlation + outbox transacional. |
| T-02-50 | Denial of Service | orphan objects | mitigate | Delete compensatório; ledger sem org FK + outbox + claim/retry duráveis. |
| T-02-SC | Tampering | provider SDK | accept | Adapter usa pacote storage auditado existente. |
</threat_model>

<verification>
- `rtk pnpm phase2:integration:preflight`
- `rtk pnpm --filter @pubg-camp/api test -- organization-logo.integration`
- `rtk pnpm --filter @pubg-camp/worker test -- logo-cleanup`
- `rtk pnpm --filter @pubg-camp/api typecheck` e `rtk pnpm --filter @pubg-camp/api build`
- `rtk pnpm --filter @pubg-camp/worker typecheck` e `rtk pnpm --filter @pubg-camp/worker build`
</verification>
<success_criteria>Upload persistente integra criação/edição/preview com autorização, URL segura, rollback e cleanup idempotente.</success_criteria>
<output>Create `.planning/phases/02-identidade-organizacoes-e-autorizacao/02-22-SUMMARY.md` when done.</output>
