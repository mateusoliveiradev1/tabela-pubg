---
phase: 02-identidade-organizacoes-e-autorizacao
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/identity.ts
  - packages/contracts/src/identity.test.ts
  - packages/contracts/src/organizations.ts
  - packages/contracts/src/organizations.test.ts
  - packages/contracts/src/audit.ts
  - packages/contracts/src/audit.test.ts
  - packages/contracts/src/index.ts
  - packages/config/src/index.ts
  - packages/config/src/index.test.ts
  - .env.example
autonomous: true
requirements: [AUTH-001, AUTH-002, AUTH-003, AUTH-004, AUTH-006, ORG-001, ORG-002, AUD-001, NFR-005]
must_haves:
  truths:
    - "Toda entrada HTTP da fase possui schema Zod e nenhuma resposta inclui tokens, códigos, digests ou ciphertext."
    - "Configuração de OAuth, sessão, OTP, CSRF, Redis, e-mail e criptografia falha no boot sem imprimir segredo."
    - "PKCE aceita somente `required` ou `documented-exception`; exception exige ID não vazio e jamais aparece sem redaction."
  artifacts:
    - path: "packages/contracts/src/identity.ts"
      provides: "Contratos de OAuth, OTP, identidades, step-up e sessões"
    - path: "packages/contracts/src/organizations.ts"
      provides: "Contratos de organizações, membros, convites e ownership"
    - path: "packages/contracts/src/audit.ts"
      provides: "Consulta paginada e projeção redigida de auditoria"
  key_links:
    - from: "packages/config/src/index.test.ts"
      to: "packages/config/src/index.ts"
      via: "config inválida e redaction"
      pattern: "REDACTED"
---

<objective>
Estabelecer contratos HTTP sem segredos e configuração segura para todos os módulos da fase.
Purpose: permitir que API, worker e web trabalhem contra formas compartilhadas e rejeitem configuração insegura cedo.
Output: schemas Zod, tipos inferidos, testes negativos e catálogo de env vars sem valores reais.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-CONTEXT.md
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-RESEARCH.md
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-UI-SPEC.md
@packages/contracts/src/index.ts
@packages/config/src/index.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Definir contratos de identidade, organizações e auditoria</name>
  <files>packages/contracts/src/identity.ts, packages/contracts/src/identity.test.ts, packages/contracts/src/organizations.ts, packages/contracts/src/organizations.test.ts, packages/contracts/src/audit.ts, packages/contracts/src/audit.test.ts, packages/contracts/src/index.ts</files>
  <behavior>
    - Request OTP aceita e-mail; response é uniforme e não revela existência, conforme D-02.
    - Session DTO expõe somente identificador público, device resumido, datas, estado atual e localização aproximada.
    - SessionAlertContext contract aceita apenas token opaco no request server-side e retorna união `active|revoked|expired|not-found`, sem session/token/digest interno.
    - Invitation DTO representa válido/expirado/revogado/usado/inválido sem retornar token; Audit DTO sempre mascara PII e exclui segredo.
  </behavior>
  <action>Criar schemas separados de input/output para OAuth, OTP, linking, step-up, sessões, organizações, convites, memberships, assignments, ownership e auditoria. Incluir `SessionAlertContextRequestSchema` e `SessionAlertContextResponseSchema` discriminado em active/revoked/expired/not-found; token só existe no request server-side e response nunca inclui IDs/digest. Usar unions para UI. Nunca incluir code/state/provider/session/invite token, digest, cookie, CSRF secret ou payload cifrado. Testar safeParse negativo e reexportar.</action>
  <verify><automated>rtk pnpm --filter @pubg-camp/contracts test &amp;&amp; rtk pnpm --filter @pubg-camp/contracts typecheck</automated></verify>
  <done>Web e API compartilham contratos completos, discriminados e livres de credenciais.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Endurecer configuração e redaction da fase</name>
  <files>packages/config/src/index.ts, packages/config/src/index.test.ts, .env.example</files>
  <behavior>
    - Segredos ausentes ou fracos falham com nomes de campos, jamais valores.
    - Redaction cobre state, code, otp, invitation, email, authorization, cookie, ciphertext e provider tokens.
    - Produção exige origem HTTPS, cookie Secure e chaves criptográficas separadas/versionadas.
    - `DISCORD_PKCE_MODE=required` inicia sem exception ID; `documented-exception` falha sem ID; valor desconhecido ou ID usado fora da exceção falha no boot.
  </behavior>
  <action>Estender Zod com `DISCORD_PKCE_MODE: z.enum(["required","documented-exception"]).default("required")` e `DISCORD_PKCE_EXCEPTION_ID` condicional: obrigatório/nonblank somente em documented-exception e proibido em required. Testar boot negativo para missing ID, valor inválido, ID no modo required e mensagem sem ID/secret; redaction inclui ambas as chaves e warnings mostram apenas que uma exceção existe. `.env.example` declara `DISCORD_PKCE_MODE=required` e deixa `DISCORD_PKCE_EXCEPTION_ID=` vazio com comentário, nunca exemplo real. Incluir origem, Discord secret/redirect, cookie/CSRF, OTP pepper, AES-GCM, Redis, Resend, TTLs e trusted proxy; casos de uso não leem process.env.</action>
  <verify><automated>rtk pnpm --filter @pubg-camp/config test &amp;&amp; rtk pnpm --filter @pubg-camp/config lint &amp;&amp; rtk pnpm verify:secrets</automated></verify>
  <done>Config insegura não inicia e nenhum erro/log de teste revela valores confidenciais.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| client→contracts | Entrada não confiável cruza schemas de request. |
| environment→runtime | Segredos operacionais entram somente no boot. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-03 | Information Disclosure | DTO/config errors | mitigate | Outputs allowlisted e testes que procuram campos secretos. |
| T-02-04 | Tampering | untrusted input | mitigate | Zod strict, limites e enums antes do domínio. |
| T-02-SC | Tampering | package installs | accept | Nenhuma dependência externa é instalada neste plano. |
</threat_model>

<verification>
- `rtk pnpm turbo run test typecheck lint --filter=@pubg-camp/contracts --filter=@pubg-camp/config`
- `rtk pnpm verify:secrets`
</verification>

<success_criteria>
- Todos os fluxos previstos no UI-SPEC têm contratos de request/response.
- AUTH-006 é verificável por ausência de campos secretos.
</success_criteria>

<output>Create `.planning/phases/02-identidade-organizacoes-e-autorizacao/02-02-SUMMARY.md` when done.</output>
