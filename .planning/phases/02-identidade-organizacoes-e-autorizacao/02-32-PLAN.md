---
phase: 02-identidade-organizacoes-e-autorizacao
plan: 32
type: execute
wave: 8
depends_on: [02-29, 02-37, 02-42]
files_modified:
  - apps/api/src/identity/session.controller.ts
  - apps/api/src/identity/session.controller.spec.ts
  - apps/api/src/identity/identity-management.controller.ts
  - apps/api/src/identity/identity-management.controller.spec.ts
  - apps/api/src/identity/identity.service.ts
  - apps/api/src/identity/identity.service.spec.ts
  - apps/api/src/identity/identity.module.ts
autonomous: true
gap_closure: true
requirements: [AUTH-003, AUTH-005, ORG-002, ORG-003, NFR-005]
must_haves:
  truths:
    - "Rotas protegidas listam/revogam/logout sessões e listam/confirmam/removem identidades usando somente request.auth."
    - "Identity change exige proof/fresh step-up, protege a última identidade e rotaciona a sessão atual/revoga as demais."
  artifacts:
    - path: "apps/api/src/identity/session.controller.ts"
      provides: "Gestão protegida completa de sessões"
    - path: "apps/api/src/identity/identity-management.controller.ts"
      provides: "Gestão protegida completa de identidades"
  key_links:
    - from: "apps/api/src/identity/identity-management.controller.ts"
      to: "packages/database/src/repositories/identity.ts"
      via: "actor/session-bound proof consume and owner-scoped mutations"
      pattern: "request.auth"
---

<objective>
Publicar os controllers e casos de uso de gestão de sessão/identidade ausentes.
Purpose: fazer revogação, logout, vínculo e remoção funcionarem no Nest real com autoridade server-side.
Output: controllers, repository/use cases e registro no IdentityModule.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-29-SUMMARY.md
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-37-SUMMARY.md
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-42-SUMMARY.md
@apps/api/src/identity/identity.module.ts
@apps/api/src/identity/session.service.ts

<interfaces>
Session endpoints: GET /identity/sessions; POST /identity/sessions/logout; POST /identity/sessions/revoke-others; POST /identity/sessions/:sessionId/revoke.
Identity endpoints: GET /identity/identities; POST /identity/identities/link/confirm; POST /identity/identities/:identityId/remove.
All protected handlers derive actorId/sessionId/trust from request.auth and validate route/body ID equality where both exist.
`IdentitySecurityChangePort` from 02-42 is the sole writer for link confirmation: proof consume, identity mutation, current opaque-token replacement, other-session revocation, trust/reauth persistence and durable evidence are not split across service calls.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Registrar controllers protegidos de sessão e identidade</name>
  <files>apps/api/src/identity/session.controller.ts, apps/api/src/identity/session.controller.spec.ts, apps/api/src/identity/identity-management.controller.ts, apps/api/src/identity/identity-management.controller.spec.ts, apps/api/src/identity/identity.service.ts, apps/api/src/identity/identity.service.spec.ts, apps/api/src/identity/identity.module.ts</files>
  <read_first>
    - packages/contracts/src/identity.ts
    - packages/database/src/repositories/identity.ts
    - apps/api/src/identity/session.service.ts
    - apps/api/src/identity/identity.service.ts
    - apps/api/src/identity/identity.module.ts
    - apps/web/src/components/identity/device-session-card.tsx
    - apps/web/src/components/identity/identity-cards.tsx
    - .planning/phases/02-identidade-organizacoes-e-autorizacao/02-CONTEXT.md (D-03, D-05, D-08, D-16)
  </read_first>
  <behavior>
    - Session list marks current, target revoke affects only actor-owned target, revoke-others preserves current, logout revokes current and clears session/CSRF cookies.
    - Identity list is redacted; link-confirm consumes the actor/session-bound pending proof once; remove rejects another user's identity and the last verified identity.
    - Link/remove require fresh step-up, rotate current token, revoke other sessions and return only allowlisted contract fields.
  </behavior>
  <action>Per D-03/D-05/D-16, add protected controllers with `@RequirePermission` metadata and request.auth-only authority. Use existing SessionService methods; when revoking current/logout, clear the session and CSRF lifecycle deterministically. Add tenant-independent identity list/remove operations scoped by userId; removal must lock/count live verified identities inside its repository transaction. For link-confirm, pass the unconsumed pending proof, actor/current-session binding and explicit candidate to the 02-42 `IdentitySecurityChangePort`; do not pre-consume the proof, mutate identity, rotate a token, revoke sessions or append evidence in this service/controller. Per D-08, set the returned replacement session cookie and rotate CSRF only after the atomic command commits; any command error emits neither cookie nor CSRF and returns no replacement token. Route/body candidate/identity IDs must agree. Register both controllers and the concrete 02-42 port adapter in `IdentityModule`.</action>
  <acceptance_criteria>
    - Controller tests cover success, other-user target, last identity, stale proof, replay, cross-session proof, stale step-up, current/other revoke and logout cookie clearing.
    - Ordering tests prove one call to the 02-42 command and post-commit replacement-cookie then CSRF; injected command failure leaves both response credentials untouched.
    - No controller accepts actorId/userId/sessionId/trust from body/query/header.
    - Every response passes shared strict schema and contains no token/digest/email plaintext/provider credential.
  </acceptance_criteria>
  <verify><automated>rtk pnpm --filter @pubg-camp/api exec vitest run src/identity/session.controller.spec.ts src/identity/identity-management.controller.spec.ts src/identity/identity.service.spec.ts &amp;&amp; rtk pnpm --filter @pubg-camp/api typecheck</automated></verify>
  <cleanup>Controller apps/fakes are closed/reset; repository integration uses a run-scoped schema and removes only its own identity/session rows.</cleanup>
  <done>Account session and identity management routes are real, protected and server-authoritative.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| browser/BFF→identity controllers | Protected management commands target user-owned security state. |

## ASVS / STRIDE Threat Register
| Threat ID | ASVS | STRIDE | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-83 | V7 | Elevation of Privilege | session revoke | mitigate | request.auth ownership and target-scoped conditional update. |
| T-02-84 | V6/V7 | Spoofing | identity link/remove | mitigate | Bound one-use proof, fresh step-up and last-identity lock. |
| T-02-86 | V14 | Information Disclosure | identity/session DTO | mitigate | Strict redacted schemas and no opaque credential projection. |
</threat_model>

<verification>
- `rtk pnpm --filter @pubg-camp/api exec vitest run src/identity/session.controller.spec.ts src/identity/identity-management.controller.spec.ts src/identity/identity.service.spec.ts`
- `rtk pnpm --filter @pubg-camp/api typecheck`
</verification>

<success_criteria>Todos os endpoints Nest de gestão existem, são protegidos e aplicam invariantes transacionais de sessão/identidade.</success_criteria>
<output>Create `.planning/phases/02-identidade-organizacoes-e-autorizacao/02-32-SUMMARY.md` when done.</output>
