---
phase: 02-identidade-organizacoes-e-autorizacao
plan: 30
type: execute
wave: 3
depends_on: [02-25, 02-27]
files_modified:
  - packages/database/src/repositories/identity.ts
  - packages/database/src/repositories/sessions.ts
  - packages/database/test/identity-organizations.integration.spec.ts
  - apps/api/src/identity/otp.service.ts
  - apps/api/src/identity/otp.service.spec.ts
autonomous: true
gap_closure: true
requirements: [AUTH-002, AUTH-003, ORG-002, NFR-005]
must_haves:
  truths:
    - "OTP sign-in cria ou resolve somente identidade email explícita e entrega um resultado privado apto a emitir uma sessão persistente."
    - "OTP não-sign-in é criado e consumido somente pelo actor/session vinculado."
    - "Coincidência de e-mail com identidade Discord nunca une contas automaticamente."
    - "Uma sessão Discord provisional autenticada torna-se trusted somente por uma transação OTP vinculada que cria/vincula a identidade email, substitui o token atual, registra reautenticação e revoga todas as outras sessões."
  artifacts:
    - path: "packages/database/src/repositories/identity.ts"
      provides: "Email identity, bound challenge consume and atomic provisional-to-trusted promotion"
  key_links:
    - from: "apps/api/src/identity/otp.service.ts"
      to: "apps/api/src/identity/identity.service.ts"
      via: "verified purpose-specific result with durable binding"
      pattern: "sign-in|step-up"
---

<objective>
Transformar o consumo OTP em prova persistente vinculada e resolução explícita de identidade de e-mail.
Purpose: preparar o acesso por e-mail e step-up sem status-only, replay ou account merge implícito.
Output: repository e serviço OTP com contratos purpose-specific comprovados em PostgreSQL real.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-25-SUMMARY.md
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-27-SUMMARY.md
@apps/api/src/identity/otp.service.ts
@packages/database/src/repositories/identity.ts

<interfaces>
Public sign-in upstream endpoints accept purpose exactly `sign-in`; protected OTP endpoints accept only link-email | change-email | step-up | verify-provisional-email.
Non-sign-in service inputs always contain `{ actorId, sessionId }` from `request.auth`; browser body cannot supply either.
Successful sign-in returns `{ userId, sessionId, sessionToken, trust: trusted }`; successful step-up returns persisted confirmation time.
Successful `verify-provisional-email` returns the replacement opaque token only after one PostgreSQL transaction has consumed the actor/session-bound challenge, created or explicitly linked the verified email identity, changed the current session to `trust=trusted`, set `reauthenticated_at`, replaced its token digest and revoked every other session per D-08.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Vincular challenges e resolver identidade email sem merge</name>
  <files>packages/database/src/repositories/identity.ts, packages/database/src/repositories/sessions.ts, packages/database/test/identity-organizations.integration.spec.ts, apps/api/src/identity/otp.service.ts, apps/api/src/identity/otp.service.spec.ts</files>
  <read_first>
    - packages/database/src/repositories/identity.ts
    - packages/database/src/repositories/sessions.ts
    - packages/database/src/schema/identity.ts
    - apps/api/src/identity/otp.service.ts
    - apps/api/src/identity/otp.service.spec.ts
    - .planning/phases/02-identidade-organizacoes-e-autorizacao/02-CONTEXT.md (D-02, D-03, D-08)
  </read_first>
  <behavior>
    - Sign-in challenge stores no actor/session; non-sign-in challenge requires and later matches both actorId and sessionId.
    - Correct code with wrong actor/session/purpose is indistinguishable from invalid and remains unusable by that caller.
    - Email sign-in resolves only provider=email subject digest; if absent creates a new user+email identity+verified email atomically, but never joins a Discord user by matching email.
    - `verify-provisional-email` accepts only the authenticated provisional Discord actor/session bound at challenge creation; consume, explicit email link/create, current-token replacement, trust promotion, `reauthenticated_at` update and other-session revocation either all commit or all roll back.
    - Replay, wrong actor/session, already-trusted session, identity/email conflict, token-digest collision and injected failure after each mutation boundary leave the old token/trust, challenge, identities and every other session unchanged.
  </behavior>
  <action>Per D-02/D-03, extend OTP repository/service contracts with durable actor/session binding returned only by conditional consume and retain `resolveOrCreateEmailAccount` keyed by the email provider subject digest for public sign-in; normalized-email coincidence must still return a stable conflict and never merge Discord accounts. Add a distinct `verify-provisional-email` use case for an authenticated provisional Discord session. In one `database.transaction`, lock the bound challenge and current session, verify actor/session/trust/purpose, consume the challenge, explicitly create or link the verified email identity to that same actor, generate and persist a replacement digest for the current opaque session token, set `trust=trusted` and `reauthenticated_at`, and revoke every other active session. This is an identity-link transition, so apply D-04 onboarding reachability and the exact D-08 rule: preserve only the session that reconfirmed both methods, while every other session ends. Return the replacement raw token only after commit; never expose it to logs/outbox. Any conflict or failure must roll back challenge consumption, identity mutation, trust/reauthentication, token replacement and other-session revocation together.</action>
  <acceptance_criteria>
    - PostgreSQL test proves correct consume, wrong-session deny, replay deny and no state mutation on mismatch.
    - Concurrent first sign-in yields one email identity/user; conflict cannot create or merge a Discord account.
    - OTP public responses remain uniform and no plaintext code enters persistence/log output.
    - PostgreSQL integration covers provisional promotion success, replay, cross-session/cross-actor denial, already-trusted denial, injected failure after identity/trust/token/revocation boundaries, and proves full rollback including continued resolution of only the old token.
    - On success the old token no longer resolves, the replacement resolves as trusted with fresh `reauthenticated_at`, every other session is revoked, and the challenge plus email identity are durably one-use/linked to the same actor.
  </acceptance_criteria>
  <verify><automated>rtk pnpm --filter @pubg-camp/api exec vitest run src/identity/otp.service.spec.ts &amp;&amp; rtk pnpm phase2:integration</automated></verify>
  <cleanup>Integration creates unique emails/IDs in a run-scoped schema and drops it in `finally`; Redis limiter keys use run prefix and are deleted by exact prefix members only.</cleanup>
  <done>OTP proof is actor/session-bound where required and email identity resolution cannot auto-merge Discord accounts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| browser→OTP endpoints | Purpose, actor and session must not be caller-selectable authority. |
| Discord/email identity graph | Email coincidence must never merge accounts. |

## ASVS / STRIDE Threat Register
| Threat ID | ASVS | STRIDE | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-75 | V6 | Spoofing | OTP binding | mitigate | Conditional actor/session/purpose consume and uniform errors. |
| T-02-76 | V6/V10 | Elevation of Privilege | account merge | mitigate | Provider-subject lookup only; conflict without implicit merge. |
</threat_model>

<verification>
- `rtk pnpm --filter @pubg-camp/api exec vitest run src/identity/otp.service.spec.ts`
- `rtk pnpm phase2:integration`
</verification>

<success_criteria>A prova OTP e a identidade de e-mail são persistidas com binding e sem merge implícito, prontas para o delivery HTTP do plano 02-36.</success_criteria>
<output>Create `.planning/phases/02-identidade-organizacoes-e-autorizacao/02-30-SUMMARY.md` when done.</output>
