---
phase: 02-identidade-organizacoes-e-autorizacao
plan: 28
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/organization-logo-origins.ts
  - apps/web/src/lib/organization-logo-origins.spec.ts
  - apps/web/next.config.ts
  - apps/web/src/app/security-headers.spec.ts
  - apps/web/src/components/identity/invitation-accept-form.tsx
  - apps/web/src/components/identity/auth.spec.tsx
  - apps/api/src/organizations/organizations.service.ts
  - apps/api/src/organizations/organization-logo.integration.spec.ts
autonomous: true
gap_closure: true
requirements: [ORG-001, NFR-005]
must_haves:
  truths:
    - "Produção permite imagens somente de origens de logo exatas, aprovadas e HTTPS; wildcard, credenciais, path/query/fragment e HTTP são rejeitados."
    - "A mesma função de origem governa CSP e validação de logo no cliente, sem políticas divergentes."
    - "Falha de signed-URL/projeção após commit nunca apaga o objeto ativo já referenciado pelo PostgreSQL."
  artifacts:
    - path: "apps/web/src/lib/organization-logo-origins.ts"
      provides: "Parser canônico de origins e diretiva CSP"
    - path: "apps/web/src/app/security-headers.spec.ts"
      provides: "Regressão de CSP exata sem wildcard"
  key_links:
    - from: "apps/web/next.config.ts"
      to: "NEXT_PUBLIC_ORGANIZATION_LOGO_ORIGINS"
      via: "validated exact origins appended to img-src"
      pattern: "img-src"
    - from: "apps/api/src/organizations/organizations.service.ts"
      to: "committed active logo metadata"
      via: "compensation boundary ends at repository commit"
      pattern: "compensat"
---

<objective>
Entregar logos assinadas com uma CSP compatível e corrigir a compensação pós-commit.
Purpose: concluir ORG-001 sem ampliar a origem de imagens nem remover objeto já comprometido.
Output: parser canônico, CSP segura, validação compartilhada e lifecycle de logo consistente.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-REVIEW.md
@apps/web/next.config.ts
@apps/web/src/components/identity/invitation-accept-form.tsx
@apps/api/src/organizations/organizations.service.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validar origens e compor CSP exata</name>
  <files>apps/web/src/lib/organization-logo-origins.ts, apps/web/src/lib/organization-logo-origins.spec.ts, apps/web/next.config.ts, apps/web/src/app/security-headers.spec.ts, apps/web/src/components/identity/invitation-accept-form.tsx, apps/web/src/components/identity/auth.spec.tsx</files>
  <read_first>
    - apps/web/next.config.ts
    - apps/web/src/app/security-headers.spec.ts
    - apps/web/src/components/identity/invitation-accept-form.tsx
    - .env.example (NEXT_PUBLIC_ORGANIZATION_LOGO_ORIGINS)
    - .planning/phases/02-identidade-organizacoes-e-autorizacao/02-CONTEXT.md (D-10, D-11)
  </read_first>
  <behavior>
    - Production accepts only comma-separated exact `https://host[:port]` origins with no credentials/path/query/fragment and rejects config atomically when any entry is invalid.
    - Development may accept exact loopback HTTP origins only; no wildcard/scheme wildcard/data expansion is generated.
    - CSP `img-src` contains self/data/blob plus deduplicated approved origins; invitation runtime uses the same parser and no-referrer.
  </behavior>
  <action>Create a server-safe pure parser and use it in both `next.config.ts` and `safeInvitationLogoUrl`; fail the production build on invalid configured entries instead of silently dropping them. Append serialized exact origins to `img-src`, preserve the exact Discord `form-action`, and do not add `https:`, `*`, wildcard subdomains, S3 bucket patterns or provider-wide origins. Keep signed URLs ephemeral and never persist them in browser storage.</action>
  <acceptance_criteria>
    - Tests cover HTTPS, duplicate normalization, IPv6/ports, credential/path/query/fragment rejection, production HTTP rejection and loopback-only dev HTTP.
    - Production CSP includes only configured exact origins and has no wildcard token.
    - A second-origin signed logo passes URL validation and renders with `referrerPolicy=no-referrer`; unapproved origin falls back locally.
  </acceptance_criteria>
  <verify><automated>rtk pnpm --filter @pubg-camp/web test -- organization-logo-origins security-headers identity/auth &amp;&amp; rtk pnpm --filter @pubg-camp/web build</automated></verify>
  <cleanup>Tests restore environment variables after each case and do not leave a modified process env or build artifact outside normal `.next` output.</cleanup>
  <done>Signed logos load from approved origins in production without weakening CSP.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Limitar compensação ao pré-commit</name>
  <files>apps/api/src/organizations/organizations.service.ts, apps/api/src/organizations/organization-logo.integration.spec.ts</files>
  <read_first>
    - apps/api/src/organizations/organizations.service.ts
    - apps/api/src/organizations/organization-logo.integration.spec.ts
    - apps/api/src/organizations/ports/organization-logo-storage.ts
    - packages/database/src/repositories/organizations.ts (logo activation/cleanup)
  </read_first>
  <behavior>
    - Put failure or repository rollback deletes/schedules only the uncommitted object.
    - Once active metadata commits, signed-URL projection failure returns safe fallback/read error and never deletes/schedules that active object.
    - Replace continues to activate new logo and enqueue prior-logo cleanup atomically.
  </behavior>
  <action>Refactor create/update logo transaction boundaries so the compensation catch surrounds only `putObject` plus the repository commit. Set an explicit committed flag/result before URL projection. After commit, project a signed URL separately; on signing failure return the configured same-origin fallback or a stable read failure without object deletion. Preserve the independent orphan-ledger path only for genuine pre-commit compensation failures.</action>
  <acceptance_criteria>
    - Integration test injects a signing failure after commit and proves active metadata/object remain and no orphan cleanup targets the active key.
    - Pre-commit DB failure still compensates safely; failed immediate delete records one independent cleanup ledger/outbox entry.
    - Existing replace/old-asset cleanup behavior remains green.
  </acceptance_criteria>
  <verify><automated>rtk pnpm --filter @pubg-camp/api test -- organization-logo &amp;&amp; rtk pnpm phase2:integration</automated></verify>
  <cleanup>Integration fixtures use run-scoped schema/object keys and remove only those keys in `finally`; validate `branding/` plus run id before deletion.</cleanup>
  <done>Committed logo state cannot be invalidated by a post-commit projection outage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| environment→CSP | Configuration controls which external image origins the browser may contact. |
| PostgreSQL commit→object storage | Compensation must distinguish uncommitted orphan from active object. |

## ASVS / STRIDE Threat Register
| Threat ID | ASVS | STRIDE | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-68 | V3/V13 | Information Disclosure | CSP img-src | mitigate | Exact HTTPS origins; fail invalid config; no wildcard. |
| T-02-69 | V14 | Information Disclosure | signed URL/referrer | mitigate | no-referrer, short TTL and no browser persistence. |
| T-02-70 | V15 | Tampering | post-commit compensation | mitigate | Explicit commit boundary and active-key regression tests. |
</threat_model>

<verification>
- `rtk pnpm --filter @pubg-camp/web test -- security-headers organization-logo-origins`
- `rtk pnpm --filter @pubg-camp/api test -- organization-logo`
- `rtk pnpm phase2:integration`
</verification>

<success_criteria>Logo real é renderizável sob CSP production-safe e o lifecycle não perde objeto confirmado.</success_criteria>
<output>Create `.planning/phases/02-identidade-organizacoes-e-autorizacao/02-28-SUMMARY.md` when done.</output>
