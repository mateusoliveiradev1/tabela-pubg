---
phase: 02-identidade-organizacoes-e-autorizacao
plan: 36
type: execute
wave: 6
depends_on: [02-27, 02-28, 02-29, 02-30, 02-31, 02-42]
files_modified:
  - apps/api/src/identity/identity.service.ts
  - apps/api/src/identity/identity.service.spec.ts
  - apps/api/src/identity/identity.runtime.ts
  - apps/api/src/identity/identity.runtime.spec.ts
  - apps/api/src/identity/identity.controller.ts
  - apps/api/src/identity/identity.controller.spec.ts
  - apps/api/src/organizations/provisional-promotion.integration.spec.ts
autonomous: true
gap_closure: true
requirements: [AUTH-002, AUTH-003, AUTH-004, ORG-002, NFR-005]
must_haves:
  truths:
    - "OTP sign-in persiste uma sessão opaca resolvível antes de emitir cookie e CSRF vinculados."
    - "OTP step-up persiste reauthenticated_at somente para a sessão autenticada vinculada antes da resposta."
    - "Rotas Nest públicas e protegidas são separadas por contrato; actor/session nunca vêm do navegador."
    - "Depois de verificar o e-mail da sessão Discord provisional, somente o token substituto trusted pode criar uma organização ou aceitar um convite para o e-mail confirmado."
  artifacts:
    - path: "apps/api/src/identity/identity.controller.ts"
      provides: "Rotas OTP públicas/protegidas com cookie e CSRF ordenados"
  key_links:
    - from: "apps/api/src/identity/identity.controller.ts"
      to: "apps/api/src/identity/session.service.ts"
      via: "request.auth-bound confirmStepUp or repository-owned session issuance"
      pattern: "confirmStepUp|startDeviceSession"
    - from: "apps/api/src/identity/identity.controller.ts"
      to: "apps/api/src/security/csrf.service.ts"
      via: "persist session/step-up before cookie and CSRF rotation"
      pattern: "rotateToSession|rotateCurrent"
---

<objective>
Entregar os contratos OTP persistentes de 02-30 pelos endpoints Nest reais.
Purpose: concluir autenticação por e-mail e step-up sem autoridade client-derived nem resposta status-only.
Output: use cases, runtime e controllers de identidade com regressões de cookie e CSRF.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-27-SUMMARY.md
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-29-SUMMARY.md
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-30-SUMMARY.md
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-31-SUMMARY.md
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-42-SUMMARY.md
@apps/api/src/identity/identity.service.ts
@apps/api/src/identity/identity.controller.ts

<interfaces>
Public sign-in accepts purpose exactly `sign-in`; protected endpoints accept only `link-email | change-email | step-up`.
Non-sign-in inputs receive `{ actorId, sessionId }` only from `request.auth`.
Successful sign-in returns a repository-owned session token; successful step-up returns persisted confirmation time.
Protected provisional-email verification receives actor/session only from `request.auth`, calls the atomic 02-30 promotion producer, replaces the opaque session cookie, then rotates CSRF; no organization or invitation route is allowlisted for provisional trust.
Ordinary `link-email | change-email` passes the still-unconsumed proof to the 02-42 atomic command and publishes the replacement cookie/CSRF only after that transaction commits.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Emitir sessão/cookie e persistir step-up por finalidade</name>
  <files>apps/api/src/identity/identity.service.ts, apps/api/src/identity/identity.service.spec.ts, apps/api/src/identity/identity.runtime.ts, apps/api/src/identity/identity.runtime.spec.ts, apps/api/src/identity/identity.controller.ts, apps/api/src/identity/identity.controller.spec.ts, apps/api/src/organizations/provisional-promotion.integration.spec.ts</files>
  <read_first>
    - apps/api/src/identity/identity.service.ts
    - apps/api/src/identity/identity.runtime.ts
    - apps/api/src/identity/identity.controller.ts
    - apps/api/src/identity/session.service.ts
    - apps/api/src/security/csrf.service.ts
    - .planning/phases/02-identidade-organizacoes-e-autorizacao/02-CONTEXT.md (D-02, D-03, D-05, D-07, D-08)
  </read_first>
  <behavior>
    - Public sign-in verify issues a trusted device session, sets its resolvable cookie, then reacquires session-bound CSRF.
    - Protected step-up derives actor/session from request.auth, consumes only the bound OTP, calls `confirmStepUp(method=email)` and rotates current CSRF only after persistence succeeds.
    - Link/change derives auth context and delegates the unconsumed bound proof to 02-42, which atomically applies D-03 identity mutation plus D-08 current-token replacement, revoke-others, trust/reauth and durable evidence.
    - Protected `verify-provisional-email` accepts only the current authenticated provisional Discord session, invokes the single atomic producer from 02-30, sets only its committed replacement token, then rotates CSRF; any failure keeps the old cookie/CSRF and durable state unchanged.
    - Before promotion, organization creation and invitation acceptance deny the provisional token; afterward, the replacement trusted token can create an organization and can accept only an invitation whose exact email was confirmed by that OTP.
  </behavior>
  <action>Per D-02/D-03, split public sign-in routes from protected non-sign-in routes so guard selection never depends on request purpose. Protected routes retain CSRF, and `@AllowProvisional()` is limited to requesting/consuming the bound verified-email OTP and logout; organization creation and invitation acceptance are never allowlisted. Wire sign-in through the 02-30 email identity result plus `startDeviceSession`, including D-07 new-device alert. For ordinary `link-email` and `change-email`, forward the still-unconsumed actor/session-bound proof and explicit candidate to the sole 02-42 command; do not consume proof, mutate identity, rotate/revoke sessions or write evidence outside it. Set its committed replacement token cookie and rotate CSRF only after success; any failure returns neither. Add the provisional Discord verification endpoint that derives actor/session/trust exclusively from `request.auth`, calls the atomic 02-30 promotion producer, and only after commit replaces the opaque session cookie with the returned token and rotates CSRF. This producer is the D-04 transition that unlocks create-organization or accept-invitation. Wire step-up to `SessionService.confirmStepUp` using persisted binding. Remove swallowed catch/status-only behavior and publish exact contracts for 02-37 without visual changes reserved for Phase 2.1.</action>
  <acceptance_criteria>
    - Controller/runtime test resolves the exact returned cookie token against persistence and sees trusted session plus new-device outbox event.
    - Positive step-up changes `reauthenticated_at` for only the bound active session before success/CSRF rotation; wrong actor/session, public step-up and replay are negative tests with no mutation.
    - Public caller cannot select link/change/step-up, and authenticated caller cannot bind another actor/session through body/header.
    - Controller tests prove replacement session-cookie then CSRF ordering, replay/cross-session/failure paths emit neither cookie nor CSRF, and the old token remains usable only when the durable transaction rolled back.
    - Ordinary email link/change tests assert one 02-42 port call with the unconsumed proof and no fallback sequence of independent proof/identity/session writers.
    - `provisional-promotion.integration.spec.ts` proves provisional create/accept denial, successful OTP promotion, organization creation with the new token, exact-confirmed-email invitation acceptance with the new token, wrong-email invitation denial, and old-token denial after promotion.
  </acceptance_criteria>
  <verify><automated>rtk pnpm --filter @pubg-camp/api exec vitest run src/identity/identity.controller.spec.ts src/identity/identity.runtime.spec.ts src/identity/identity.service.spec.ts src/organizations/provisional-promotion.integration.spec.ts &amp;&amp; rtk pnpm --filter @pubg-camp/api typecheck</automated></verify>
  <cleanup>Controller tests clear cookies/fakes and close runtime Redis; no real email is sent, and run-scoped outbox/delivery rows are removed only with their isolated schema.</cleanup>
  <done>Email OTP establishes a durable trusted session or a durable bound step-up through the real HTTP path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| browser→OTP endpoints | Purpose, actor and session must not be caller-selectable authority. |
| verified challenge→identity/session | One-use proof creates authentication state and cookies. |

## ASVS / STRIDE Threat Register
| Threat ID | ASVS | STRIDE | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-77 | V7 | Session Fixation | cookie issuance | mitigate | Repository-owned 256-bit token + rotateToSession then CSRF. |
| T-02-78 | V3/V7 | Tampering | step-up/CSRF order | mitigate | Persist reauthenticatedAt before CSRF rotation/success. |
| T-02-95 | V6 | Spoofing | OTP purpose/actor binding | mitigate | Separate public/protected routes and request.auth-only authority. |
| T-02-106 | V7 | Session Fixation/Elevation of Privilege | provisional promotion | mitigate | Atomic bound promotion, old-token replacement, other-session revocation, post-commit cookie/CSRF order and rollback tests. |
</threat_model>

<verification>
- `rtk pnpm --filter @pubg-camp/api exec vitest run src/identity/identity.controller.spec.ts src/identity/identity.runtime.spec.ts src/identity/identity.service.spec.ts src/organizations/provisional-promotion.integration.spec.ts`
- `rtk pnpm phase2:integration`
</verification>

<success_criteria>AUTH-002 e step-up email funcionam pelo runtime real sem alterar o escopo visual reservado à Fase 2.1.</success_criteria>
<output>Create `.planning/phases/02-identidade-organizacoes-e-autorizacao/02-36-SUMMARY.md` when done.</output>
