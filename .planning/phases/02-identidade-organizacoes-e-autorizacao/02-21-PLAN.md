---
phase: 02-identidade-organizacoes-e-autorizacao
plan: 21
type: execute
wave: 6
depends_on: [02-05]
files_modified:
  - packages/contracts/src/organizations.ts
  - packages/contracts/src/organizations.spec.ts
  - packages/storage/src/index.ts
  - packages/storage/src/organization-logo.ts
  - packages/storage/src/organization-logo.test.ts
  - packages/database/src/schema/organizations.ts
  - packages/database/src/schema.ts
  - packages/database/migrations/0002_organization_logos.sql
  - packages/database/migrations/meta/_journal.json
  - packages/database/migrations/meta/0002_snapshot.json
  - packages/database/src/repositories/organizations.ts
  - packages/database/test/organization-logo.integration.spec.ts
autonomous: true
requirements: [ORG-001, NFR-005]
must_haves:
  truths:
    - "Logo aceita somente PNG/JPEG/WebP dentro do limite e a assinatura real deve coincidir com o MIME declarado."
    - "Object key opaca é gerada somente no servidor no namespace branding; filename, bucket e key nunca entram no contrato público."
    - "Banco persiste metadados e vínculo tenant-aware; bytes permanecem no object storage."
    - "Falha de persistência mantém o logo anterior ativo; se delete imediato falhar, ledger órfão independente persiste e libera retry sem depender de organização existente."
  artifacts:
    - path: "packages/storage/src/organization-logo.ts"
      provides: "Inspeção de assinatura e adapter seguro reutilizando validateUpload/createObjectKey"
    - path: "packages/database/migrations/0002_organization_logos.sql"
      provides: "Metadados de assets e ledger independente de cleanup órfão"
    - path: "packages/contracts/src/organizations.ts"
      provides: "Contratos multipart/resposta e job cleanup somente por cleanupId"
  key_links:
    - from: "packages/storage/src/organization-logo.ts"
      to: "packages/storage/src/index.ts"
      via: "validateUpload + createObjectKey(branding) antes de PutObject"
      pattern: "validateUpload|createObjectKey"
    - from: "StorageCleanupRepository export in packages/database/src/repositories/organizations.ts"
      to: "packages/database/src/schema/organizations.ts"
      via: "activateLogo troca metadata por organization_id na transação"
      pattern: "organizationLogo"
    - from: "packages/database/src/repositories/organizations.ts"
      to: "orphan_storage_cleanup_ledger + outbox"
      via: "enqueueOrphanCleanup persiste ledger e evento por cleanupId em transação independente"
      pattern: "enqueueOrphanCleanup"
---

<objective>
Criar a fundação persistente e segura para logos de organização.
Purpose: completar ORG-001/NFR-005 sem confiar em MIME/filename do browser nem armazenar URLs efêmeras no banco.
Output: contratos, inspeção/storage adapter, schema/migration e repository testados.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-RESEARCH.md
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-PATTERNS.md
@packages/storage/src/index.ts
@packages/database/src/repositories/organizations.ts
@packages/contracts/src/organizations.ts

<interfaces>
Do storage existente: `validateUpload(size, mimeType, policy)`, `createObjectKey(namespace, organizationId, objectId?)`, `createStorage(config)` e `ObjectKeySchema`. Reusar estes exports; não aceitar chave ou namespace do cliente.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Definir contratos e validar bytes reais no storage</name>
  <files>packages/contracts/src/organizations.ts, packages/contracts/src/organizations.spec.ts, packages/storage/src/index.ts, packages/storage/src/organization-logo.ts, packages/storage/src/organization-logo.test.ts</files>
  <behavior>
    - Multipart aceita `name` e `logo` opcional na criação; edição aceita um único `logo`; respostas retornam somente id, detectedMime, byteSize e URL recuperável.
    - `OrganizationLogoCleanupJobSchema` aceita somente cleanupId opaco; rejeita objectKey/provider/organizationId no payload.
    - PNG/JPEG/WebP válidos até 2 MiB passam; zero bytes, oversized, SVG/HTML, polyglot conhecido e MIME spoofed falham antes de upload.
    - Key usa `branding/{organizationUUID}/{objectUUID}` gerada pelo servidor e nunca usa filename/path recebido.
  </behavior>
  <action>Adicionar schemas de metadata/response e limites multipart, sem modelar bytes em JSON/base64, e `OrganizationLogoCleanupJobSchema` contendo exclusivamente cleanupId UUID opaco. Criar `inspectOrganizationLogo` que chama `validateUpload`, confere magic bytes/estrutura mínima para PNG/JPEG/WebP e exige igualdade entre MIME declarado e detectado. Estender o adapter existente com `putObject` e `deleteObject` validados por ObjectKeySchema, além do download assinado já existente. `storeOrganizationLogo` gera key por `createObjectKey("branding", organizationId)` e calcula SHA-256; não expor config, bucket, key ou filename.</action>
  <verify><automated>rtk pnpm --filter @pubg-camp/contracts test -- organizations &amp;&amp; rtk pnpm --filter @pubg-camp/storage test -- organization-logo</automated></verify>
  <done>Contratos multipart/response e inspeção server-side rejeitam oversized/spoofed MIME e produzem key opaca somente para bytes válidos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrar metadados de logo tenant-aware</name>
  <files>packages/database/src/schema/organizations.ts, packages/database/src/schema.ts, packages/database/migrations/0002_organization_logos.sql, packages/database/migrations/meta/_journal.json, packages/database/migrations/meta/0002_snapshot.json</files>
  <behavior>
    - Cada asset ativo pertence a uma organização e registra key única, MIME detectado, tamanho, SHA-256, creator e timestamps.
    - No máximo um asset ACTIVE por organização; PENDING/DELETE_PENDING suportam compensação sem tornar URL pública persistente.
    - FK/unique/checks impedem key cross-tenant, tamanho inválido ou MIME fora da allowlist.
    - `orphan_storage_cleanup_ledger` possui cleanup UUID opaco, provider, object key server-side, digest, status, attempts, nextAttemptAt/claimedAt/completedAt/createdAt e nenhuma FK de organização.
  </behavior>
  <action>Adicionar `organization_logo_assets` e referência ativa tenant-aware, com enums/status e partial unique para um ACTIVE por org. Na mesma migration criar tabela independente `orphan_storage_cleanup_ledger`, deliberadamente sem organization FK, com cleanup UUID opaco, provider allowlisted, object_key server-side, object_key_digest, status pending/claimed/completed/failed, attempts e next_attempt_at/claimed_at/completed_at/created_at/updated_at; índices suportam claim retry e object digest idempotente. Banco nunca guarda URL assinada ou bytes. Criar migration incremental 0002 e snapshot/journal determinísticos.</action>
  <verify><automated>rtk pnpm phase2:integration:preflight &amp;&amp; rtk pnpm verify:migrations &amp;&amp; rtk pnpm --filter @pubg-camp/database db:check</automated></verify>
  <done>Migration do zero e incremental preservam organizações existentes e garantem um logo ativo tenant-aware.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Persistir ativação, substituição e cleanup</name>
  <files>packages/database/src/repositories/organizations.ts, packages/database/test/organization-logo.integration.spec.ts</files>
  <behavior>
    - `activateLogo` exige organizationId explícito, metadata validada e executor transacional; devolve asset anterior sem apagar o novo.
    - Erro/concorrência não deixa duas linhas ACTIVE nem troca o logo anterior parcialmente.
    - `enqueueOrphanCleanup` recebe somente key criada pelo storage server-side e grava ledger+outbox atomicamente; job payload contém apenas cleanupId.
    - Claim por cleanupId/nextAttempt é idempotente; complete/retry incrementam attempts e nunca exigem organizationId.
  </behavior>
  <action>Estender OrganizationRepository somente com createPendingLogo/activateLogo/getActiveLogo e exportar um `StorageCleanupRepository` separado no mesmo módulo para `enqueueOrphanCleanup`, `claimOrphanCleanup`, `completeOrphanCleanup` e `retryOrphanCleanup`; sua API não recebe organizationId nem exige organização existente. Enqueue abre transação independente e grava ledger + outbox `storage.logo.cleanup` contendo somente cleanupId; valida ObjectKeySchema no boundary e calcula digest server-side, sem aceitar key de request/job. Claim usa lock/lease e nextAttemptAt. Nenhum repository chama S3 ou gera URL. Testar PostgreSQL real, duas organizações, org inexistente, rollback, enqueue atômico, claim concorrente, retry e complete idempotentes.</action>
  <verify><automated>rtk pnpm phase2:integration:preflight &amp;&amp; rtk pnpm --filter @pubg-camp/database test -- organization-logo.integration storage-cleanup-ledger</automated></verify>
  <done>Metadata/troca são tenant-aware e o ledger órfão independente garante enqueue, claim, retry e conclusão duráveis.</done>
</task>
</tasks>

<execution_notes>
File ownership/commits: Task 1 possui contracts/storage; Task 2 possui schema/migration; Task 3 possui repository/integration. Criar commits atômicos nesta ordem (`feat(02-21): secure logo contracts and storage`, `feat(02-21): persist logo metadata`, `feat(02-21): transact logo activation`) e não misturar migration com API/UI.
</execution_notes>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| multipart bytes→storage | Nome, MIME, tamanho e bytes são não confiáveis. |
| storage key→database tenant | Key válida não prova ownership da organização. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-44 | Tampering | MIME/bytes | mitigate | Allowlist, limite e magic-byte inspection server-side. |
| T-02-45 | Elevation of Privilege | object key | mitigate | Key server-generated e vínculo organization_id/FK. |
| T-02-46 | Denial of Service | multipart/orphan object | mitigate | Streaming/2 MiB hard limit e ledger independente com retry/lease. |
| T-02-SC | Tampering | storage dependencies | accept | Reusa SDK/pacote storage já auditado na Fase 1; nenhuma dependência nova. |
</threat_model>

<verification>
- `rtk pnpm --filter @pubg-camp/storage test -- organization-logo`
- `rtk pnpm phase2:integration:preflight`
- `rtk pnpm --filter @pubg-camp/database test -- organization-logo.integration storage-cleanup-ledger`
</verification>
<success_criteria>Logo possui contratos seguros, bytes inspecionados, key opaca server-side, metadata tenant-aware e ledger órfão independente com outbox/retry duráveis.</success_criteria>
<output>Create `.planning/phases/02-identidade-organizacoes-e-autorizacao/02-21-SUMMARY.md` when done.</output>
