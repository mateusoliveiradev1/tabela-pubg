---
phase: 02-identidade-organizacoes-e-autorizacao
plan: 34
type: execute
wave: 12
depends_on: [02-39]
files_modified: []
autonomous: false
gap_closure: true
requirements: [AUTH-001, AUTH-002, AUTH-003, AUTH-004, AUTH-005, AUTH-006, ORG-001, ORG-002, ORG-003, ORG-004, ORG-005, AUD-001, NFR-005]
must_haves:
  truths:
    - "O HEAD final executa um único workflow event=push autorizado com o gate real PostgreSQL+Redis+Nest+publisher+worker+browser em success."
    - "Nenhuma suite obrigatória é skipped, neutral, cancelled ou substituída por synthetic API."
    - "Os oito jobs obrigatórios correspondem exatamente aos key/matrix/name pairs canônicos do workflow rastreado; evidência não pode autodeclarar cardinalidade menor ou substituições."
    - "Evidência registra branch/SHA/run/jobs sem segredos e cleanup confirma ausência de schema, fila e diretório do run."
  artifacts:
    - path: ".planning/phases/02-identidade-organizacoes-e-autorizacao/02-34-SUMMARY.md"
      provides: "Machine-readable sole authority for the post-remediation CI run and cleanup evidence"
  key_links:
    - from: "git HEAD autorizado"
      to: "GitHub Actions CI"
      via: "exact branch + event=push + headSha cardinality one"
      pattern: "headSha"
    - from: "GitHub Actions runtime job"
      to: "scripts/run-phase2-e2e.mjs"
      via: "real-stack mandatory command"
      pattern: "test:e2e:runtime"
    - from: ".planning/phases/02-identidade-organizacoes-e-autorizacao/02-34-SUMMARY.md"
      to: "scripts/validate-phase2-validation.mjs"
      via: "single structured Phase 2 Post-Remediation Evidence object consumed by 02-40"
      pattern: "Phase 2 Post-Remediation Evidence"
---

<objective>
Executar a evidência final imutável depois de todos os planos de remediação.
Purpose: impedir encerramento da Fase 2 por testes locais parciais, run antigo ou workflow sintético.
Output: checkpoint autorizado com run único do HEAD exato e todas as suites reais verdes.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-39-SUMMARY.md
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-41-SUMMARY.md
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-42-SUMMARY.md
@.github/workflows/ci.yml
@scripts/validate-phase2-ci.mjs
@scripts/run-phase2-e2e.mjs
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-VERIFICATION.md

<interfaces>
02-34-SUMMARY must contain exactly one machine-readable JSON object under the exact heading `## Phase 2 Post-Remediation Evidence`. The object is the sole final authority consumed only by 02-40 and contains: `schemaVersion`; `planCoverage.prerequisiteSummaries` equal to the exact 16-file set `02-25-SUMMARY.md` through `02-33-SUMMARY.md` plus `02-35-SUMMARY.md` through `02-39-SUMMARY.md` plus `02-41-SUMMARY.md` and `02-42-SUMMARY.md` (never its own future `02-34-SUMMARY.md` or ledger-only `02-40-SUMMARY.md`), and `planCoverage.closurePlan` equal to `02-40`; `branch`; `headSha`; workflow fields `event`, `cardinality`, `runId`, `runUrl`, `attempt`, `status`, and `conclusion`; `mandatoryJobCount`; unique `mandatoryJobs[]` entries with `key`, optional exact `matrix`, `name`, and `conclusion`; runtime fields `jobName`, `command`, `runScopeId`, `runScopeSource`, `components`, and `syntheticApi`; cleanup entries `schema`, `redisBullmqPrefix`, and `objectMailRoot`, each with exact `identifier`, matching `scopeId`, and `removed`; and re-verification fields `truthsPassed`, `truthsTotal`, `requirementsPassed`, and `requirementsTotal`.

The canonical mandatory expanded job set is derived from and locked against the tracked `.github/workflows/ci.yml`, cardinality exactly 8: `quality` / no matrix / `Quality and affected build`; `phase2-integration` / `suite=integration` / `Phase 2 integration`; `phase2-integration` / `suite=concurrent` / `Phase 2 concurrent`; `phase2-e2e` / no matrix / `Phase 2 browser E2E`; and `images` / `app=web|api|worker|discord-bot` / `Image web|Image api|Image worker|Image discord-bot`. The workflow definition and GitHub job response must both match this set; `mandatoryJobCount` is only a mirrored value and never the source of expected cardinality.

`runtime.runScopeId` is the exact CI `E2E_RUN_ID`: 19-67 ASCII characters matching `^run-[a-z0-9][a-z0-9-]{14,62}$`, not matching broad `^run-(all|any|default|shared|global|public|phase2|e2e|test)(-|$)`, and sourced from `.github/workflows/ci.yml#jobs.phase2-e2e.env.E2E_RUN_ID` as recorded in `runtime.runScopeSource`. Each cleanup scopeId equals it byte-for-byte; matching invalid or broad cleanup values remain invalid.
The runtime command is exactly `rtk pnpm test:e2e:runtime`; components are exactly Next+BFF, Nest, PostgreSQL, Redis, outbox publisher, BullMQ consumers, worker, and browser; `runtime.syntheticApi` is false. The historical run 32676449341 and SHA a41f87c539435ed00dd848571699e5ebede8f97c may appear only outside this object as superseded history.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <evidence_contract>Populate the single JSON object defined in the context only from the tracked workflow, exact `gh` run/job response and run-scoped cleanup logs. Require the exact 16 prerequisite summaries excluding 02-34 and 02-40; event=push, cardinality=1, status=completed, conclusion=success; and set equality with the exact eight canonical key/matrix/name jobs from context, each conclusion success. Treat mandatoryJobCount=8 only as a mirrored cross-check. Require exact command `rtk pnpm test:e2e:runtime`, the exact eight-component real stack, syntheticApi=false, and a runScopeId sourced from the phase2-e2e E2E_RUN_ID that satisfies the exact pattern, 19-67 length and broad-sentinel denial. Require all three cleanup entries with non-broad identifiers, scopeId equal byte-for-byte to runtime.runScopeId and removed=true, plus re-verification 6/6 truths and 13/13 requirements. Reject run 32676449341, SHA a41f87c539435ed00dd848571699e5ebede8f97c, and all pre-remediation values from the authoritative object. Preserve those old values only in separate prose labeled superseded. Redact credentials, env values, OTP/invitation/provider tokens and mailbox contents.</evidence_contract>
  <name>Task 1: Autorizar e provar o CI pós-remediação do HEAD exato</name>
  <files>Nenhum arquivo é modificado neste checkpoint.</files>
  <read_first>
    - .planning/phases/02-identidade-organizacoes-e-autorizacao/02-39-SUMMARY.md
    - .planning/phases/02-identidade-organizacoes-e-autorizacao/02-41-SUMMARY.md
    - .planning/phases/02-identidade-organizacoes-e-autorizacao/02-42-SUMMARY.md
    - .github/workflows/ci.yml
    - scripts/validate-phase2-ci.mjs
    - scripts/run-phase2-e2e.mjs
    - .planning/phases/02-identidade-organizacoes-e-autorizacao/02-VERIFICATION.md
  </read_first>
  <action>Run local static/global gates first. Confirm clean intended diff and that exactly the prerequisite summaries 02-25 through 02-33, 02-35 through 02-39, 02-41 and 02-42 exist; never include this checkpoint's future 02-34-SUMMARY or ledger-only 02-40-SUMMARY in `prerequisiteSummaries`. Parse the tracked workflow and record the exact eight canonical key/matrix/name pairs from context before capturing branch plus `PHASE2_GAP_HEAD_SHA` with `rtk git rev-parse HEAD`. Ask explicit authorization before the exact `rtk git push` that triggers CI; do not use workflow_dispatch. Query workflow runs by branch + event=push + exact headSha and require cardinality one, then watch with exit status/timeout. Inspect the GitHub jobs and require exact set equality against those eight workflow-derived pairs and success for every entry; reject missing, renamed/replaced, duplicate or unexpected mandatory entries even if evidence adjusts its count. Inspect migration fresh+upgrade, PostgreSQL/Redis integration, D-08 transactional failure-injection/replay evidence, concurrency, route inventory, worker publisher, real runtime E2E, browser/accessibility, secret scan, build and cleanup evidence. Capture runtime.runScopeId from the actual phase2-e2e E2E_RUN_ID evidence and enforce its source/pattern/length/non-broad contract plus exact cleanup scope equality, including the 02-41 auth-limiter and PKCE namespaces. Reject run 32676449341 or any pre-remediation SHA as stale. Record in 02-34-SUMMARY the exact branch, SHA, run ID/URL, canonical job pairs/conclusions, real-stack command, run scope source/value, cleanup identifiers/results and re-verification score, redacting service URLs/env/mailbox data; only 02-40 parses this immutable handoff to update the validation ledger.</action>
  <what-built>Fase 2 remediada com publisher durável, identidade/sessão/OAuth/tenant/controllers/CSP e gate real do stack.</what-built>
  <how-to-verify>1. `rtk node scripts/validate-phase2-ci.mjs .github/workflows/ci.yml` passa e prova ausência de synthetic mandatory API. 2. `rtk pnpm ci:verify` passa. 3. Branch e HEAD SHA são capturados. 4. Usuário autoriza explicitamente o push. 5. Exatamente um run event=push corresponde ao SHA. 6. `rtk gh run watch ... --exit-status` termina success antes do timeout. 7. Jobs obrigatórios executam, incluindo `test:e2e:runtime`, sem skip. 8. Logs comprovam cleanup run-scoped e não contêm credenciais/OTP/invite/provider tokens. 9. Reexecutar a verificação da fase reporta 6/6 truths e 13/13 requirements.</how-to-verify>
  <acceptance_criteria>
    - Run é do HEAD pós-02-39/02-41/02-42, event=push, branch exata, cardinalidade um e conclusion success; prerequisiteSummaries is the exact 16-file set excluding 02-34/02-40.
    - Mandatory jobs equal the exact eight tracked workflow key/matrix/name pairs and all conclude success; self-declared count changes or renamed replacements cannot pass.
    - Job real-stack prova Nest+PostgreSQL+Redis+publisher+worker+browser; synthetic API não aparece como evidência obrigatória.
    - Nova verificação mantém satisfeitos AUTH-001/AUTH-004/AUTH-006/ORG-004/ORG-005/AUD-001 e fecha os sete requisitos bloqueados.
    - SUMMARY registra 02-25–02-33, 02-35–02-39 e 02-41–02-42, SHA/run/jobs/runScope/cleanup e identifica 32676449341 como evidência histórica synthetic-only que não encerra a remediação.
  </acceptance_criteria>
  <verify><automated>rtk node scripts/validate-phase2-ci.mjs .github/workflows/ci.yml &amp;&amp; rtk pnpm ci:verify &amp;&amp; rtk gh run view "$PHASE2_GAP_RUN_ID" --json databaseId,url,headSha,event,status,conclusion,jobs</automated></verify>
  <cleanup>Before approval, verify the CI finally-step removed the exact run schema, BullMQ/Redis prefix and temporary object/mail root. If cleanup failed, keep the checkpoint blocked and remove only validated run-scoped targets; never delete shared/public schema, unprefixed Redis keys or broad filesystem roots.</cleanup>
  <resume-signal>Autorize explicitamente o push e aprove somente após o run único do HEAD exato terminar success com cleanup confirmado.</resume-signal>
  <done>Run pós-remediação imutável prova todos os requisitos da Fase 2 em stack real e a reverificação fecha 6/6 e 13/13.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| workspace→GitHub remote | Push é ação externa autorizada e identifica o código provado. |
| CI evidence→phase closure | Run/SHA/jobs incorretos não podem liberar a fase. |

## ASVS / STRIDE Threat Register
| Threat ID | ASVS | STRIDE | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-91 | V16 | Tampering/Repudiation | stale/ambiguous CI run | mitigate | Exact branch/event/headSha and cardinality one. |
| T-02-92 | V15 | Tampering | skipped/synthetic gate | mitigate | Static validator + mandatory job inspection. |
| T-02-93 | V14 | Information Disclosure | CI logs/artifacts | mitigate | Redaction and explicit secret/mailbox scan. |
| T-02-94 | V13/V15 | Tampering/DoS | failed cleanup | mitigate | Run-scoped proof; checkpoint remains blocked until safe cleanup. |
</threat_model>

<verification>Somente um run autorizado event=push do HEAD exato, integralmente success e seguido de reverificação 6/6 + 13/13 conclui o plano.</verification>
<success_criteria>A Fase 2 possui evidência final pós-remediação, imutável, real-stack e sem gaps verificados.</success_criteria>
<output>Create `.planning/phases/02-identidade-organizacoes-e-autorizacao/02-34-SUMMARY.md` when done.</output>
