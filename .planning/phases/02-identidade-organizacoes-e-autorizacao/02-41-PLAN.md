---
phase: 02-identidade-organizacoes-e-autorizacao
plan: 41
type: execute
wave: 7
depends_on: [02-36]
files_modified:
  - apps/api/src/identity/identity.runtime.ts
  - apps/api/src/identity/identity.runtime.spec.ts
  - apps/api/src/identity/adapters/redis-auth-rate-limiter.ts
  - apps/api/src/identity/adapters/redis-auth-rate-limiter.integration.spec.ts
  - scripts/run-phase2-integration.mjs
autonomous: true
gap_closure: true
requirements: [AUTH-001, AUTH-002, AUTH-003, NFR-005]
must_haves:
  truths:
    - "Identity runtime derives both auth-limiter and OAuth-PKCE Redis prefixes from one validated runScopeId in test/E2E mode."
    - "Production runtime keeps the stable pubg-camp:auth and pubg-camp:oauth:pkce defaults when no run scope is requested."
    - "Concurrent test/E2E runs cannot consume, rate-limit or clean another run's keys; cleanup removes only the exact validated scope."
    - "The real Redis spec never skips: preflight validates the same REDIS_URL/TEST_REDIS_URL selected by the spec, and missing Redis or zero selected tests exits nonzero."
  artifacts:
    - path: "apps/api/src/identity/identity.runtime.ts"
      provides: "Validated production-or-run Redis scope contract injected into both identity adapters"
    - path: "apps/api/src/identity/adapters/redis-auth-rate-limiter.ts"
      provides: "Explicit prefix constructors for auth limiter and OAuth PKCE keys"
    - path: "apps/api/src/identity/adapters/redis-auth-rate-limiter.integration.spec.ts"
      provides: "Two-run isolation and exact-prefix cleanup proof against Redis"
  key_links:
    - from: "apps/api/src/identity/identity.runtime.ts"
      to: "apps/api/src/identity/adapters/redis-auth-rate-limiter.ts"
      via: "one validated scope produces and injects authKeyPrefix and oauthPkceKeyPrefix"
      pattern: "runScopeId|authKeyPrefix|oauthPkceKeyPrefix"
    - from: "apps/api/src/identity/adapters/redis-auth-rate-limiter.integration.spec.ts"
      to: "Redis"
      via: "exact run-prefixed key enumeration and cleanup with foreign-scope sentinel preservation"
      pattern: "scan|unlink|runScopeId"
---

<objective>
Make every identity Redis key explicitly production-stable or uniquely run-scoped.

Purpose: prevent mandatory integration/E2E runs from sharing the hardcoded OAuth PKCE and auth-limiter namespaces currently constructed by `createIdentityRuntime`.
Output: validated runtime prefix injection, explicit adapter prefixes and real Redis isolation/cleanup tests.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-36-SUMMARY.md
@apps/api/src/identity/identity.runtime.ts
@apps/api/src/identity/adapters/redis-auth-rate-limiter.ts
@apps/api/src/identity/adapters/redis-auth-rate-limiter.integration.spec.ts
@scripts/run-phase2-integration.mjs
@.env.example
@.github/workflows/ci.yml

<interfaces>
`createIdentityRuntime` currently constructs `new RedisDiscordOAuthVerifierStore(redis, "pubg-camp:oauth:pkce")` and `new RedisAuthRateLimiter(redis)`; the latter defaults to `pubg-camp:auth`. Both prefixes therefore collide across real concurrent runs.

Add an explicit discriminated runtime input: production/default mode resolves exactly `authKeyPrefix="pubg-camp:auth"` and `oauthPkceKeyPrefix="pubg-camp:oauth:pkce"`; run mode requires `runScopeId` satisfying the canonical 02-39 contract (`^run-[a-z0-9][a-z0-9-]{14,62}$`, 19-67 ASCII characters, excluding `^run-(all|any|default|shared|global|public|phase2|e2e|test)(-|$)`) and resolves both prefixes beneath one byte-identical scope, for example `pubg-camp:${runScopeId}:auth` and `pubg-camp:${runScopeId}:oauth:pkce`.

02-33 owns API entrypoint/fake-provider composition and must pass run mode under the complete E2E conjunction. 02-39 owns the full harness and must propagate the same E2E_RUN_ID to runtime and cleanup.

Repository convention is `REDIS_URL` in `.env.example`, runtime and both CI service jobs. The Redis integration spec may use an explicit `TEST_REDIS_URL`; when absent it must select `REDIS_URL`. If `TEST_REDIS_URL` is present it is the selected test endpoint and preflight must validate/connect to it too. Blank, malformed, placeholder or unreachable selected URLs fail before Vitest; no environment state may convert the suite to skipped.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inject one validated scope into both identity Redis adapters</name>
  <files>apps/api/src/identity/identity.runtime.ts, apps/api/src/identity/identity.runtime.spec.ts, apps/api/src/identity/adapters/redis-auth-rate-limiter.ts, apps/api/src/identity/adapters/redis-auth-rate-limiter.integration.spec.ts, scripts/run-phase2-integration.mjs</files>
  <read_first>
    - apps/api/src/identity/identity.runtime.ts
    - apps/api/src/identity/identity.runtime.spec.ts
    - apps/api/src/identity/adapters/redis-auth-rate-limiter.ts
    - apps/api/src/identity/adapters/redis-auth-rate-limiter.integration.spec.ts
    - scripts/run-phase2-e2e.mjs
    - scripts/run-phase2-integration.mjs
    - .env.example
    - .github/workflows/ci.yml
    - .planning/phases/02-identidade-organizacoes-e-autorizacao/02-39-PLAN.md
  </read_first>
  <behavior>
    - Production/default runtime explicitly passes the stable current prefixes to both constructors; this mode does not synthesize a run id.
    - Test/E2E run mode rejects absent, malformed, broad, whitespace/path-bearing, under-length and over-length runScopeId before either adapter can write Redis.
    - A valid runScopeId deterministically yields distinct auth-limiter and PKCE sub-prefixes under the same exact run scope, and runtime passes those values to the actual adapter instances.
    - Two live Redis adapter sets with different runScopeIds cannot see/consume each other's PKCE state or share limiter counters.
    - Exact-scope cleanup removes all and only keys beneath one validated run prefix; a foreign-run key and an unscoped production sentinel remain untouched.
    - The integration spec has an unconditional `describe`, selects `TEST_REDIS_URL` when explicitly present and otherwise `REDIS_URL`, and throws before test registration/connection on missing or invalid input; it contains no `describe.runIf`, `it.runIf`, skip or todo escape.
    - Preflight validates and PINGs the exact selected test Redis endpoint before the exact Vitest selector; missing/unreachable Redis and a selector yielding zero tests both return nonzero.
  </behavior>
  <action>Create one exported validator/prefix resolver in `identity.runtime.ts` (or the adapter module if that avoids a cycle) and use it before constructing Redis adapters. Replace the hardcoded PKCE constructor argument and default limiter construction with explicit resolved prefixes. Keep the existing production values byte-for-byte stable; never derive a test prefix from a generic `NODE_ENV`, timestamp, worker id or broad default. Run mode must require caller-supplied validated `runScopeId`, and both prefixes must contain the same value exactly once. Tighten both adapter constructors/options so blank, broad or separator-escaping prefixes cannot silently fall back. Extend runtime unit tests with constructor/client spies proving exact injection and fail-before-write behavior. In `scripts/run-phase2-integration.mjs`, preserve mandatory `REDIS_URL`, additionally reject an explicitly present blank/invalid `TEST_REDIS_URL`, resolve the test endpoint as `TEST_REDIS_URL ?? REDIS_URL`, and PING that exact selected endpoint during preflight (deduplicate only when byte-identical). In the Redis integration spec use the same resolution rule, validate it unconditionally at module setup, remove conditional `describe.runIf` and every skip/todo path, and connect or throw. Add two independently generated canonical run IDs and prove limiter-counter isolation, PKCE cross-run consume denial, same-run one-use/replay denial, and guarded SCAN+UNLINK cleanup that first validates the exact scope and preserves foreign-run plus production sentinel keys. Do not use Redis `KEYS`, `FLUSHDB`, a bare `pubg-camp:*` delete, cleanup derived from invalid scope, or `--passWithNoTests`.</action>
  <acceptance_criteria>
    - Unit tests assert exact stable production defaults and exact valid run-derived prefixes for both constructors.
    - Every invalid/broad run scope rejects before Redis connection writes, and test/E2E mode has no implicit shared fallback.
    - Redis integration proves independent limiter counters, PKCE one-use within its own run, cross-run consume failure, and exact cleanup with both foreign sentinels preserved.
    - 02-33 can pass the validated E2E_RUN_ID into this contract without constructing prefixes itself, while 02-39 can use the same value for its cleanup evidence.
    - Source scan finds no conditional/skip escape in the exact Redis spec, and the documented exact command runs preflight before that spec without `--passWithNoTests`.
  </acceptance_criteria>
  <verify><automated>rtk pnpm --filter @pubg-camp/api exec vitest run src/identity/identity.runtime.spec.ts &amp;&amp; rtk pnpm phase2:integration:preflight &amp;&amp; rtk pnpm --filter @pubg-camp/api exec vitest run src/identity/adapters/redis-auth-rate-limiter.integration.spec.ts &amp;&amp; rtk pnpm --filter @pubg-camp/api typecheck</automated></verify>
  <cleanup>Integration cleanup validates its exact runScopeId, scans/unlinks only `pubg-camp:${runScopeId}:*`, asserts the foreign-run and production sentinel keys still exist, then removes only the test-owned sentinels explicitly and closes clients in `finally`.</cleanup>
  <done>Identity Redis state is production-stable by default and uniquely isolated plus safely cleanable for every real test/E2E run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| runtime config→Redis adapters | One validated scope must govern both limiter and PKCE namespaces. |
| test cleanup→shared Redis | Cleanup must prove exact ownership before deleting keys. |
| environment/preflight→Redis spec | The URL health-checked before execution must be the URL the non-skippable spec actually uses. |

## ASVS / STRIDE Threat Register
| Threat ID | ASVS | STRIDE | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-107 | V7/V10 | Spoofing/Tampering | shared PKCE or limiter namespace | mitigate | Explicit validated run prefix injected into both adapters with two-run isolation tests. |
| T-02-108 | V13/V15 | Tampering/DoS | Redis cleanup | mitigate | Validated exact-prefix SCAN+UNLINK and foreign-sentinel preservation; no broad delete. |
| T-02-109 | V14 | Information Disclosure | raw OAuth state in key names | mitigate | Preserve SHA-256 state digest beneath the scoped PKCE prefix. |
| T-02-114 | V15 | Tampering | skipped/vacuous Redis evidence | mitigate | Shared URL resolution, real PING preflight, unconditional suite and exact no-zero-test selector. |
</threat_model>

<verification>
- `rtk pnpm --filter @pubg-camp/api exec vitest run src/identity/identity.runtime.spec.ts`
- `rtk pnpm phase2:integration:preflight`
- `rtk pnpm --filter @pubg-camp/api exec vitest run src/identity/adapters/redis-auth-rate-limiter.integration.spec.ts`
- `rtk pnpm --filter @pubg-camp/api typecheck`
</verification>

<success_criteria>Both identity Redis adapters receive one explicit validated scope, production defaults remain stable, and real Redis tests prove run isolation plus exact cleanup.</success_criteria>
<output>Create `.planning/phases/02-identidade-organizacoes-e-autorizacao/02-41-SUMMARY.md` when done.</output>
