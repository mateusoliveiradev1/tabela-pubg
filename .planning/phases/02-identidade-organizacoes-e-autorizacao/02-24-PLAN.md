---
phase: 02-identidade-organizacoes-e-autorizacao
plan: 24
type: execute
wave: 14
depends_on: [02-18]
files_modified: []
autonomous: false
requirements: [AUTH-001, AUTH-002, AUTH-003, AUTH-004, AUTH-005, AUTH-006, ORG-001, ORG-002, ORG-003, ORG-004, ORG-005, AUD-001, NFR-005]
must_haves:
  truths:
    - "Workflow final, após todas as alterações de CI/suites/harness, executa integration, concorrência, smoke e E2E completo em run real verde."
    - "Workflow é disparado exclusivamente pelo push autorizado da branch da fase; dispatch manual é proibido."
    - "Run deve corresponder exatamente a branch, event=push e HEAD SHA capturado; ausência, ambiguidade, failure, skipped, cancelled ou timeout nunca concluem."
    - "SUMMARY registra branch/HEAD SHA/run URL/id/conclusion e jobs obrigatórios sem segredo; somente então 02-20 pode iniciar."
  artifacts: []
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/run-phase2-integration.mjs + scripts/run-phase2-e2e.mjs"
      via: "workflow final com PostgreSQL/Redis, concurrent, smoke antes de full E2E"
      pattern: "phase2:integration|test:e2e:smoke|test:e2e"
    - from: "git HEAD SHA autorizado"
      to: "GitHub Actions run"
      via: "push event + branch + headSha exact match"
      pattern: "headSha"
---

<objective>
Executar e aprovar o workflow CI final depois de todas as suites e alterações de YAML.
Purpose: separar disponibilidade inicial de infraestrutura da evidência final imutável consumida pela matriz Nyquist.
Output: checkpoint non-autonomous com workflow validado e run GitHub Actions verde.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.github/workflows/ci.yml
@scripts/validate-phase2-ci.mjs
@scripts/run-phase2-integration.mjs
@scripts/run-phase2-e2e.mjs
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-18-SUMMARY.md
</context>

<tasks>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Autorizar e provar o workflow CI final</name>
  <files>Nenhum arquivo é modificado neste checkpoint.</files>
  <action>Primeiro executar `rtk node scripts/validate-phase2-ci.mjs .github/workflows/ci.yml` e provar que o workflow final preserva `pull_request`, possui `on.push.branches` com `main` e também inclui exatamente `phase/02-identidade-organizacoes-e-autorizacao`, sem plano posterior que altere o YAML. Confirmar branch limpa, definir `$PHASE2_CI_BRANCH='phase/02-identidade-organizacoes-e-autorizacao'` e capturar `$PHASE2_CI_HEAD_SHA` por `rtk git rev-parse HEAD`. Pedir autorização explícita antes de `rtk git push origin HEAD:refs/heads/phase/02-identidade-organizacoes-e-autorizacao`; sem autorização, parar pendente. Dispatch manual não é permitido. Após o push, consultar `rtk gh run list --workflow ci.yml --branch "$PHASE2_CI_BRANCH" --event push --limit 20 --json databaseId,url,headSha,event,status,conclusion,createdAt`, filtrar localmente por branch/event/headSha e exigir exatamente um run; zero ou múltiplos matches falham. Guardar ID em `$PHASE2_CI_RUN_ID`, aguardar com `rtk gh run watch "$PHASE2_CI_RUN_ID" --exit-status` com timeout explícito e então inspecionar jobs/logs. Provar migration/preflight PostgreSQL+Redis, integration, invitation/last-owner concurrency, cleanup ledger, upload/rollback, browser smoke e E2E completo. Não concluir com SHA diferente, run antigo/ambíguo, skipped, cancelled, timeout, neutral ou non-success.</action>
  <what-built>Workflow final contendo todos os testes da fase, smoke rápido antes do E2E completo e services reais.</what-built>
  <how-to-verify>1. Validador prova `pull_request` preservado e `push.branches` contendo `main` mais a branch exata da fase no YAML final. 2. HEAD SHA é capturado antes do push. 3. Usuário autoriza explicitamente o push. 4. `rtk git push` dispara o workflow; nenhum dispatch manual é usado. 5. `rtk gh run list` encontra exatamente um event=push com branch/headSha capturados. 6. `rtk gh run watch --exit-status` retorna zero antes do timeout. 7. Jobs obrigatórios mostram integration/concurrent/smoke/full E2E executados, não skipped. 8. SUMMARY registra branch, SHA, URL/id/conclusion e jobs, com env redigido.</how-to-verify>
  <resume-signal>Autorize explicitamente o push da branch da fase e aprove somente depois que o run do HEAD SHA exato terminar success.</resume-signal>
  <verify><automated>rtk node scripts/validate-phase2-ci.mjs .github/workflows/ci.yml &amp;&amp; rtk gh run view "$PHASE2_CI_RUN_ID" --json databaseId,url,headSha,event,status,conclusion,jobs</automated></verify>
  <done>Push autorizado gerou exatamente um run event=push do HEAD SHA capturado; watch terminou success e todas as suites foram provadas.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| workspace→GitHub remote | Push/workflow alteram estado externo e exigem autorização. |
| CI evidence→Nyquist | Run incorreto ou parcial não pode liberar flags. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-53 | Tampering | stale/partial/ambiguous run | mitigate | Branch+event+HEAD SHA exatos, cardinalidade 1 e jobs obrigatórios não skipped. |
| T-02-54 | Repudiation | push/run | mitigate | Autorização registrada antes de ação externa. |
| T-02-55 | Information Disclosure | CI logs | mitigate | Env/URLs redigidos e artifacts sem secrets. |
| T-02-SC | Tampering | CI actions/services | mitigate | YAML final validado e actions pinadas antes do run. |
</threat_model>

<verification>Checkpoint aprovado somente com push autorizado e run único event=push do HEAD SHA exato em `success`, provando integration, concurrent, smoke e E2E completo.</verification>
<success_criteria>02-20 depende obrigatoriamente deste gate e nenhuma falha/skip/timeout é aceita como conclusão.</success_criteria>
<output>Create `.planning/phases/02-identidade-organizacoes-e-autorizacao/02-24-SUMMARY.md` when done.</output>
