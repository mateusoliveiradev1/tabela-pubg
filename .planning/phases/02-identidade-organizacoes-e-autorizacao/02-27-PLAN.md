---
phase: 02-identidade-organizacoes-e-autorizacao
plan: 27
type: execute
wave: 2
depends_on: [02-25]
files_modified:
  - packages/database/src/repositories/sessions.ts
  - packages/database/test/session-alert-context.integration.spec.ts
  - apps/api/src/identity/session.service.ts
  - apps/api/src/identity/session.service.spec.ts
  - apps/api/src/identity/identity.runtime.ts
  - apps/api/src/identity/identity.runtime.spec.ts
  - apps/api/src/main.ts
autonomous: true
gap_closure: true
requirements: [AUTH-003, NFR-005]
must_haves:
  truths:
    - "O token opaco devolvido é exatamente o token cujo digest foi persistido."
    - "Cada request válida aplica sliding expiry coalescida sem ultrapassar o teto absoluto."
  artifacts:
    - path: "packages/database/src/repositories/sessions.ts"
      provides: "Emissão single-owner e resolve-and-touch com trust"
  key_links:
    - from: "apps/api/src/main.ts"
      to: "packages/database/src/repositories/sessions.ts"
      via: "authenticate→resolveAndTouchSession"
      pattern: "resolveAndTouchSession"
    - from: "apps/api/src/identity/session.service.ts"
      to: "apps/api/src/identity/identity.runtime.ts"
      via: "repository owns and returns the persisted opaque token"
      pattern: "issued.token"
---

<objective>
Fechar emissão, resolução, expiração deslizante e trust das sessões opacas.
Purpose: fazer o cookie emitido ser utilizável e impedir privilégio completo antes da verificação de e-mail.
Output: repository/runtime coerentes e testes reais de lifecycle.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-25-SUMMARY.md
@packages/database/src/repositories/sessions.ts
@apps/api/src/identity/session.service.ts
@apps/api/src/identity/identity.runtime.ts
@apps/api/src/main.ts

<interfaces>
Authenticated request context becomes `{ actorId, sessionId, trust }`.
Trusted sessions: 30-day sliding idle, 90-day absolute cap, 5-minute touch coalescing (D-06).
Provisional sessions: 15-minute absolute/idle cap and explicit route allowlist only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unificar origem do token e resolve-and-touch</name>
  <files>packages/database/src/repositories/sessions.ts, packages/database/test/session-alert-context.integration.spec.ts, apps/api/src/identity/session.service.ts, apps/api/src/identity/session.service.spec.ts, apps/api/src/identity/identity.runtime.ts, apps/api/src/identity/identity.runtime.spec.ts, apps/api/src/main.ts</files>
  <read_first>
    - packages/database/src/repositories/sessions.ts
    - packages/database/test/session-alert-context.integration.spec.ts
    - apps/api/src/identity/session.service.ts
    - apps/api/src/identity/identity.runtime.ts
    - apps/api/src/main.ts
    - .planning/phases/02-identidade-organizacoes-e-autorizacao/02-CONTEXT.md (D-05, D-06, D-07)
  </read_first>
  <behavior>
    - `issueSessionForDevice` is the sole token generator/owner and runtime returns its `issued.token`; service no longer manufactures a discarded token.
    - All issue paths persist explicit trust and return a token resolvable to the exact row digest.
    - Authentication atomically resolves active user/session/device/trust and coalesces touch; idle expiry moves by 30 days but never beyond absolute expiry.
  </behavior>
  <action>Per D-05/D-06/D-07, change `SessionRepositoryPort.issueForDevice` to return the repository-issued token and remove its token input and duplicate `SessionService` generation. Persist trust in `issueIdentitySession`/`issueSessionForDevice`. Replace the production `resolveSession` call with a repository `resolveAndTouchSession` that returns session+device+trust whether the write wins or is coalesced; keep active-user, revoked, idle and absolute predicates in both paths. Inject clock for integration assertions and never rotate on ordinary touch.</action>
  <acceptance_criteria>
    - Contract test hashes returned token and finds exactly the inserted session; a different generated token does not resolve.
    - Touch before coalescing cutoff performs no write; after cutoff advances lastSeen/idle; absolute cap is unchanged.
    - Revoked, idle-expired, absolute-expired and suspended-user sessions remain unresolved and untouched.
  </acceptance_criteria>
  <verify><automated>rtk pnpm --filter @pubg-camp/api exec vitest run src/identity/session.service.spec.ts src/identity/identity.runtime.spec.ts &amp;&amp; rtk pnpm phase2:integration</automated></verify>
  <cleanup>Integration rows use a run-scoped PostgreSQL schema and are dropped in `finally`; never revoke or update caller-owned sessions.</cleanup>
  <done>Every issued cookie resolves to its persisted row and valid activity enforces D-06 sliding expiry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| opaque cookie→session repository | Browser credential must resolve to one durable digest and lifecycle. |

## ASVS / STRIDE Threat Register
| Threat ID | ASVS | STRIDE | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-64 | V7 | Spoofing | token issuance | mitigate | Single token owner + digest-resolution contract test. |
| T-02-65 | V7 | Elevation of Privilege | sliding expiry | mitigate | Resolve-and-touch predicates and absolute cap. |
</threat_model>

<verification>
- `rtk pnpm --filter @pubg-camp/api exec vitest run src/identity/session.service.spec.ts src/identity/identity.runtime.spec.ts`
- `rtk pnpm phase2:integration`
- `rtk pnpm --filter @pubg-camp/api build`
</verification>

<success_criteria>Sessões emitidas são resolvíveis, renováveis conforme D-06 e limitadas pelo trust persistido.</success_criteria>
<output>Create `.planning/phases/02-identidade-organizacoes-e-autorizacao/02-27-SUMMARY.md` when done.</output>
