---
phase: 02-identidade-organizacoes-e-autorizacao
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - packages/database/src/schema/identity.ts
  - packages/database/src/schema/organizations.ts
  - packages/database/src/schema/authorization.ts
  - packages/database/src/schema/audit.ts
  - packages/database/src/schema/notifications.ts
  - packages/database/src/schema.ts
  - packages/database/migrations/0001_identity_organizations.sql
  - packages/database/migrations/meta/_journal.json
  - packages/database/migrations/meta/0001_snapshot.json
  - package.json
  - scripts/run-phase2-integration.mjs
  - scripts/validate-phase2-ci.mjs
  - vitest.integration.config.ts
  - .github/workflows/ci.yml
autonomous: true
requirements: [AUTH-001, AUTH-002, AUTH-003, AUTH-004, AUTH-005, AUTH-006, ORG-001, ORG-002, ORG-003, ORG-004, ORG-005, AUD-001, NFR-005]
must_haves:
  truths:
    - "Tokens de sessão/convite/OAuth e OTP nunca são persistidos em texto puro."
    - "Contextos de alerta de novo dispositivo são opacos, digest-backed, user-bound, read-only e expiráveis."
    - "Toda linha pertencente a tenant carrega organization_id e vínculos compostos impedem scope/membership de outra organização."
    - "O schema suporta sessões multi-dispositivo, convites single-use, roles escopadas e auditoria append-only."
    - "Nenhuma suite PostgreSQL/Redis inicia sem preflight verde contra serviços reais e migrations aplicadas."
  artifacts:
    - path: "packages/database/migrations/0001_identity_organizations.sql"
      provides: "Schema PostgreSQL completo e reversível da fase"
    - path: "packages/database/src/schema/identity.ts"
      provides: "users, identities, verified_emails, challenges, oauth transactions, devices e sessions"
    - path: "packages/database/src/schema/organizations.ts"
      provides: "organizations, memberships e invitations tenant-aware"
    - path: "scripts/run-phase2-integration.mjs"
      provides: "Gate fail-closed de PostgreSQL/Redis real antes das suites de integração"
  key_links:
    - from: "packages/database/src/schema/authorization.ts"
      to: "packages/database/src/schema/organizations.ts"
      via: "FKs compostas organization + membership/scope"
      pattern: "organizationId"
---

<objective>
Criar o modelo persistente completo de identidade, organizações, autorização, auditoria e notificações.
Purpose: colocar isolamento, unicidade e integridade de credenciais no PostgreSQL antes dos repositories.
Output: schemas Drizzle modulares, migration explícita e journal consistente.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-RESEARCH.md
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-PATTERNS.md
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-VALIDATION.md
@packages/database/src/schema.ts
@packages/database/migrations/0000_foundation.sql
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Modelar identidade, sessões e notificações cifradas</name>
  <files>packages/database/src/schema/identity.ts, packages/database/src/schema/notifications.ts, packages/database/src/schema.ts</files>
  <behavior>
    - Unique provider+subject impede duplicação de identidade; e-mail normalizado e verificado é inequívoco.
    - Challenges/OAuth/sessions e session_alert_contexts guardam somente HMAC/digest e lifecycle timestamps; notification delivery guarda envelope AES-GCM versionado e expirável.
    - Sessões representam device, idle/absolute expiry, reauthenticatedAt, revokedAt/reason e lastSeen coalescido.
  </behavior>
  <action>Implementar tabelas e enums descritos no Proposed Data Model, incluindo `session_alert_contexts(user_id, session_id, token_digest, expires_at, resolved_at)` cujo resolve é read-only e nunca autoriza revogação. Criar índices para lookup de digest/expiração/status e nenhuma coluna de token/code plaintext. Separar device signal de autenticação. Reexportar módulos no schema barrel preservando outbox existente. Constraints devem suportar consumo/rotação condicionais e D-05/D-06/D-08.</action>
  <verify><automated>rtk pnpm --filter @pubg-camp/database typecheck &amp;&amp; rtk pnpm --filter @pubg-camp/database test</automated></verify>
  <done>Persistência de autenticação suporta lifecycle completo sem armazenar credenciais reutilizáveis.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Modelar tenants, convites, scopes, roles e auditoria</name>
  <files>packages/database/src/schema/organizations.ts, packages/database/src/schema/authorization.ts, packages/database/src/schema/audit.ts, packages/database/src/schema.ts</files>
  <behavior>
    - Um usuário possui memberships independentes em várias organizações por D-09.
    - Convite possui digest único, e-mail normalizado, payload de role, 7 dias e estados accepted/revoked/superseded.
    - Role assignment não referencia membership ou authorization scope de organização diferente.
  </behavior>
  <action>Criar organizations, memberships, invitations, authorization_scopes(kind tournament), role_assignments e audit_events. Usar chaves/uniques/FKs compostas para provar tenant comum e índices por organization. Audit event contém actor, timestamp, reason, before/after, correlation e scope, sem update/delete exposto pela aplicação. Não antecipar entidade Tournament; o scope mínimo terá ID estável que a Fase 3 reutilizará.</action>
  <verify><automated>rtk pnpm --filter @pubg-camp/database typecheck &amp;&amp; rtk pnpm lint:boundaries</automated></verify>
  <done>O banco impede relações cross-tenant e representa D-09–D-17 sem domínio de campeonato antecipado.</done>
</task>

<task type="auto">
  <name>Task 3: Gerar migration e criar gate executável PostgreSQL/Redis</name>
  <files>packages/database/migrations/0001_identity_organizations.sql, packages/database/migrations/meta/_journal.json, packages/database/migrations/meta/0001_snapshot.json, package.json, scripts/run-phase2-integration.mjs, scripts/validate-phase2-ci.mjs, vitest.integration.config.ts, .github/workflows/ci.yml</files>
  <action>Gerar/revisar migration e manter journal/snapshot sincronizados. Criar `phase2:integration:preflight` que exige URLs reais, abre PostgreSQL/Redis, valida health, aplica migrations do zero em schema isolado e falha — nunca pula — se indisponível. Criar config Vitest integration serial e scripts que sempre executam preflight. Na CI, provisionar PostgreSQL/Redis com healthchecks e rodar preflight antes de integration/concurrent. Criar `validate-phase2-ci.mjs` que valida estrutura YAML exigida (job, services, healthchecks, env, frozen install, preflight e suites) e falha em placeholder/skip/continue-on-error. A execução real e eventual autorização de push ficam bloqueadas no 02-17; este task não declara infraestrutura aprovada.</action>
  <verify><automated>rtk pnpm verify:migrations &amp;&amp; rtk pnpm --filter @pubg-camp/database db:check &amp;&amp; rtk node scripts/validate-phase2-ci.mjs .github/workflows/ci.yml</automated></verify>
  <done>Migration e workflow são estruturalmente válidos; nenhuma suite integration está liberada até o checkpoint 02-17 aprovar serviços e CI reais.</done>
</task>
</tasks>

<execution_notes>
File ownership/commits: Task 1 possui schema identity/notifications; Task 2 possui organizations/authorization/audit; Task 3 possui migration/meta, scripts e workflow. Alterações compartilhadas em `schema.ts` são seriadas na ordem das tasks. Criar um commit atômico por task e deixar `.github/workflows/ci.yml` exclusivamente no commit da Task 3.
</execution_notes>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| API→PostgreSQL | Estado autenticado e tenant-owned torna-se persistente. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-05 | Information Disclosure | credential columns | mitigate | Somente digest/HMAC/AES-GCM e teste de busca por plaintext. |
| T-02-06 | Elevation of Privilege | tenant relations | mitigate | organization_id obrigatório e FKs compostas. |
| T-02-SC | Tampering | migration tooling | mitigate | Lockfile existente, drizzle-kit auditado na Fase 1 e `verify:migrations`. |
</threat_model>

<verification>
- `rtk pnpm verify:migrations`
- `rtk pnpm turbo run lint typecheck test build --filter=@pubg-camp/database`
</verification>

<success_criteria>
- Schema cobre todas as entidades obrigatórias da fase e preserva a outbox existente.
- Nenhum segredo reutilizável aparece em colunas persistentes.
</success_criteria>

<output>Create `.planning/phases/02-identidade-organizacoes-e-autorizacao/02-03-SUMMARY.md` when done.</output>
