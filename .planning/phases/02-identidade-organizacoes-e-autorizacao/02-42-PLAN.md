---
phase: 02-identidade-organizacoes-e-autorizacao
plan: 42
type: execute
wave: 5
depends_on: [02-25, 02-27, 02-30, 02-31]
files_modified:
  - packages/database/src/repositories/identity-security-change.ts
  - packages/database/src/repositories/index.ts
  - packages/database/test/identity-security-change.integration.spec.ts
autonomous: true
gap_closure: true
requirements: [AUTH-002, AUTH-003, AUTH-006, NFR-005]
must_haves:
  truths:
    - "Uma vinculação de identidade ou troca ordinária de e-mail consome a prova vinculada, altera a identidade, substitui o token opaco da sessão atual, revoga todas as outras sessões e persiste trust/reauth/outbox em uma única transação PostgreSQL."
    - "Falha em qualquer fronteira reverte prova, identidade, sessão atual, outras sessões e evento durável; nenhum estado parcial ou novo token utilizável escapa."
    - "Prova de outra sessão/ator e replay são negados, enquanto somente o novo token da sessão reconfirmada resolve após commit."
  artifacts:
    - path: "packages/database/src/repositories/identity-security-change.ts"
      provides: "Comando transacional D-08 e contrato/porta única para mudanças ordinárias de identidade"
    - path: "packages/database/test/identity-security-change.integration.spec.ts"
      provides: "Failure injection, replay e isolamento cross-session em PostgreSQL real"
  key_links:
    - from: "packages/database/src/repositories/identity-security-change.ts"
      to: "identity proof, identities, sessions and outbox repositories"
      via: "one database.transaction callback and one transaction executor"
      pattern: "transaction|consume|rotate|revoke|outbox"
    - from: "packages/database/test/identity-security-change.integration.spec.ts"
      to: "PostgreSQL"
      via: "real commit/rollback assertions against every durable boundary"
      pattern: "failure|replay|cross-session|rollback"
---

<objective>
Criar a única fronteira transacional para vínculo de identidade e troca ordinária de e-mail.
Purpose: cumprir D-08 sem permitir proof consumed, identity changed ou session rotation parcialmente persistidos.
Output: porta/comando de repository exportado e regressões PostgreSQL de commit, rollback, replay e sessão cruzada.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-25-SUMMARY.md
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-27-SUMMARY.md
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-30-SUMMARY.md
@.planning/phases/02-identidade-organizacoes-e-autorizacao/02-31-SUMMARY.md
@packages/database/src/repositories/identity.ts
@packages/database/src/repositories/sessions.ts
@packages/database/src/outbox.ts
@packages/database/src/repositories/index.ts

<interfaces>
Export one `IdentitySecurityChangePort`/command with explicit `database`, `actorId`, `currentSessionId`, `proofId`, `change` (`link-identity | change-email` with provider-specific candidate), `now`, correlation/id generators and a 32-byte opaque-token generator. It returns the committed current `sessionId`, the new raw opaque token and the count of revoked other sessions; it never returns proof digests, token digests, plaintext OTP or provider credentials.

The command owns `database.transaction(...)`. Every helper receives that same transaction executor; no service/controller may consume the proof, mutate identity or rotate/revoke sessions before or after this command.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Produzir mudança de identidade D-08 em uma transação PostgreSQL</name>
  <files>packages/database/src/repositories/identity-security-change.ts, packages/database/src/repositories/index.ts, packages/database/test/identity-security-change.integration.spec.ts</files>
  <read_first>
    - packages/database/src/repositories/identity.ts
    - packages/database/src/repositories/sessions.ts
    - packages/database/src/outbox.ts
    - packages/database/src/repositories/audit.ts
    - packages/database/src/repositories/index.ts
    - packages/database/test/identity-organizations.integration.spec.ts
    - .planning/phases/02-identidade-organizacoes-e-autorizacao/02-CONTEXT.md (D-03, D-05, D-08)
  </read_first>
  <behavior>
    - Success consumes exactly one unexpired proof bound to the same actor/current active session and requested change, then mutates the identity, replaces only the current session token digest, persists `reauthenticated_at=now` and the authoritative trust state, revokes every other active session, and appends the redacted durable security outbox/audit record before one commit.
    - A throw injected after proof consume, identity mutation, current-session rotate/trust persistence, other-session revocation, or audit/outbox append rolls back every earlier write; the candidate raw token never resolves after rollback and the original proof/token/session set remains authoritative.
    - Wrong actor, wrong current session, stale proof, purpose/candidate mismatch and replay fail before durable mutation; two concurrent consumes have exactly one winner.
    - After commit the old current token and every other session token fail resolution, only the returned replacement token resolves to the same session/user with fresh reauthentication and intended trust, and replay cannot change identity or emit a second event.
    - The exact PostgreSQL spec is unconditional after the real preflight; missing DATABASE_URL/PostgreSQL, conditional skip or zero selected tests exits nonzero.
  </behavior>
  <action>Per D-03/D-08, implement a narrowly scoped repository command and exported port that opens one PostgreSQL transaction and passes its executor through the existing proof, identity, session and outbox/audit helpers. Lock/recheck the actor-owned current active session and bound proof inside the transaction. Conditionally consume the proof, apply only the requested explicit identity link/email replacement without email-coincidence merge, hash/persist a newly generated 256-bit opaque token for the current session, persist fresh `reauthenticated_at` plus the resulting trust state, revoke all other active sessions, and append the required redacted durable `identity.security-state-changed` event using the existing outbox/audit boundary before returning. Account-only changes must not fabricate an organization business-audit row; if the existing audit schema is tenant-bound, the redacted outbox/security event is the applicable durable record. Generate the raw replacement token before entering the transaction but return/use it only after commit. Any missing/zero-row conditional update throws so the transaction rolls back. Export the command from the repository barrel for 02-32/36/37; prohibit orchestration that performs any listed write in separate transactions.</action>
  <acceptance_criteria>
    - Integration tests use PostgreSQL real and assert the complete committed row/event/token state for both `link-identity` and `change-email`.
    - Named failure-injection cases exist after each of the five durable boundaries and assert byte-for-byte equivalent pre-command state plus zero new durable events.
    - Named cross-session, wrong-actor, mismatch, stale, replay and two-consumer race cases deny without partial writes; the concurrent case records one identity mutation/event only.
    - Token assertions resolve old current, every other session and replacement tokens through the real session repository, proving exactly the replacement survives on commit and no replacement survives rollback.
    - The spec contains no `describe.runIf`, skip, todo or `--passWithNoTests`; its exact selector follows preflight and fails if PostgreSQL is absent or the file registers zero tests.
  </acceptance_criteria>
  <verify><automated>rtk pnpm phase2:integration:preflight &amp;&amp; rtk pnpm --filter @pubg-camp/database exec vitest run test/identity-security-change.integration.spec.ts</automated></verify>
  <cleanup>Use a unique run-scoped PostgreSQL schema, inject faults only through test-owned hooks on the command boundary, remove only that schema in `finally`, and never log raw proof/session tokens or service URLs.</cleanup>
  <done>D-08 identity link/email change is one all-or-nothing PostgreSQL command with exhaustive boundary rollback, replay and cross-session evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| service→database command | Actor/session/proof/change enter one authoritative durable command. |
| proof/identity→session state | Security identity and all session credentials must change atomically. |
| transaction→outbox/security audit | Durable evidence must commit or rollback with the mutation. |

## ASVS / STRIDE Threat Register
| Threat ID | ASVS | STRIDE | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-110 | V6/V7 | Spoofing/Elevation of Privilege | bound proof consume | mitigate | Actor/session/purpose/candidate lock and conditional one-use consume inside transaction. |
| T-02-111 | V7 | Session Fixation | current token rotation | mitigate | 256-bit opaque replacement digest, old-token invalidation and revoke-all-others in the same transaction. |
| T-02-112 | V16 | Repudiation/Tampering | partial identity change | mitigate | Transactional redacted outbox/security evidence plus failure injection at every boundary. |
| T-02-113 | V14 | Information Disclosure | token/proof/event data | mitigate | Digest-only persistence and allowlisted event fields without email/token/provider credentials. |
</threat_model>

<verification>
- `rtk pnpm phase2:integration:preflight`
- `rtk pnpm --filter @pubg-camp/database exec vitest run test/identity-security-change.integration.spec.ts`
</verification>

<success_criteria>Ordinary identity link/email-change either commits proof, identity, replacement session, other-session revocation and durable evidence together or changes nothing.</success_criteria>
<output>Create `.planning/phases/02-identidade-organizacoes-e-autorizacao/02-42-SUMMARY.md` when done.</output>
